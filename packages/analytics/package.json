{
  "name": "@app-starter/analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "pnpm --filter @app-starter/schema build",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "pretypecheck": "pnpm --filter @app-starter/schema build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "pretest": "pnpm --filter @app-starter/schema build",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@app-starter/schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}

# 前端开发工程师 · 面试与学习知识文档

| 属性 | 说明 |
|------|------|
| 适用岗位 | 前端开发工程师（3–5 年，本科，深圳 / 广州） |
| 文档用途 | 面试官出题与评分；候选人系统自学与复盘 |
| 对应职责 | Web 产品开发维护、技术选型与架构、设计稿还原、前后端联调、代码质量、性能与兼容、技术调研 |
| 文档版本 | v1.1（每题补全标准答案与参考实现） |

---

## 如何使用本文档

每道面试题固定结构：**题目 → 标准答案 → 参考实现（如有）→ 追问标准答**。  
标准答案按 3–5 年候选人 1～2 分钟口述来写，可直接对照评分或复述学习。不必逐字相同，但结论、原理、边界三者缺一则视为未答完。

**面试官**

- 先看 [§0 能力模型](#0-岗位能力模型与面试权重)，按候选人履历选 1 个主框架（Vue / React / Angular）深挖，不要三套都问透。
- 一场 45–60 分钟面试建议：基础 15 分钟 + 框架原理 15 分钟 + 工程与性能 10 分钟 + 项目深挖 15 分钟。
- ★ 初级、★★ 中级、★★★ 资深。3–5 年以 ★★ 为主，★★★ 用于区分 offer 档位。
- 对照标准答案看：结论对不对、原理清不清、有没有边界与取舍。

**学习者**

- 按 [§13 学习路径](#13-四周自学路径) 推进。每题先合上文档口述，再对照标准答案查漏。
- 带代码的题（深拷贝、并发池、debounce、迷你响应式）要能默写。
- 面试官会换场景追问；标准答案里标出的反直觉点是追问高发区。用自己的项目替换文中案例。

---

## 0. 岗位能力模型与面试权重

岗位 JD 拆成可考核的能力层。权重用于分配面试时间，不是招聘打分公式。

| 能力层 | JD 原话映射 | 面试权重 | 3–5 年期望 |
|--------|-------------|----------|------------|
| JavaScript / HTML / CSS 基础 | HTML5、CSS3、ES6+，计算机基础 | 20% | 能讲清运行时、异步、布局与渲染，不停留在 API 记忆 |
| 框架与生态 | React / Vue / Angular 至少一门，状态与路由，**核心原理** | 25% | 会用且能讲虚拟 DOM / 响应式 / 调度；能对比另一门框架 |
| 工程化 | Webpack、Vite、Babel，项目构建 | 15% | 能讲打包链路、按需加载、常见优化，不只会 `npm run build` |
| 性能与兼容 | 性能优化、PC / 移动端兼容 | 15% | 有量化手段（LCP、CLS 等），能讲移动端 1px、适配、兼容策略 |
| 协作与质量 | Git、Code Review、接口约定、设计稿还原 | 15% | 能讲规范、联调契约、可维护代码，而不是「我会用 Git」 |
| 架构与自驱 | 技术选型、高可用可扩展、新技术落地 | 10% | 能讲一次选型或一次性能治理的完整闭环 |

**岗位职责对应的行为题（建议至少问 2 道）**

**B1. 设计稿还原：如何保证还原度？和设计师意见不一致时怎么处理？**

**标准答案：**  
保证还原度靠流程，不靠眼力。落地前先对齐 Design Token：色板、字号层级、间距 8 的倍数、圆角和阴影档位。PC / Mobile 若是两套稿，就做两套布局组件，而不是只改 `font-size`。开发阶段用标注工具对齐，全局 `box-sizing: border-box`，注意亚像素、字体基线和 1px 线。提测前按走查清单过：间距、对齐、hover/active、空态、超长文案、不同 dpr。有争议时先复现：用同一台设备和同一浏览器对比设计稿。若差 1～2px 且用户不可感知，记录规范差异，避免每个页面各调一次。若差异可感知（对比度不够、点击热区过小、动画掉帧），用实现成本说话：例如高斯模糊 + 滚动吸附在低端安卓上掉到 20fps，给替代方案（减模糊半径或改 CSS `backdrop-filter` 为静态底），让设计做取舍而不是直接拒绝。

**B2. 前后端协作：接口未就绪时如何并行开发？字段变更如何治理？**

**标准答案：**  
先定契约再写页面。用 OpenAPI / Apifox 把路径、方法、请求头、分页、错误码、时间格式、金额单位定死，前端用 MSW 或 mock 服务按契约开发，并用 openapi-typescript 生成类型。字段变更禁止静默改语义：新增字段向后兼容；改名或改类型走版本（`/v2`）或同时返回新旧字段并给下线时间。发现不一致的手段至少要有一种：类型 CI、运行时 zod 校验、或联调环境的契约测试。联调时单独处理跨域、Cookie、网关前缀和环境 hosts，不要把环境问题当成业务 bug。

**B3. 代码质量：你主导或推动过哪些规范 / Review 卡点？**

**标准答案示例：**  
我推动过三件事。第一，ESLint + Prettier 进 CI，格式之争退出 Review。第二，Review 卡点写成清单：列表必须稳定 key、禁止把密钥和 `.env` 提交进库、异步必须处理失败态、禁止未转义的 `innerHTML` / `dangerouslySetInnerHTML`、组件超过 300 行或职责混杂必须拆。第三，接口层统一封装错误码和超时，页面不准各自 `try/catch` 后再静默 `console.log`。效果是线上 XSS 和「按钮点了没反应」类问题明显下降。格式问题交给工具，人只看正确性、边界和可维护性。

**B4. 性能：最近一次让页面变快的具体手段、指标前后对比。**

**标准答案示例：**  
活动落地页移动端 LCP 实验室分数 4.2s。Performance 里 LCP 元素是首屏 2000px 宽的 PNG 轮播，且 JS 包 1.8MB 阻塞。手段：轮播改成一张 WebP 英雄图 + `preload`，其余图懒加载；路由懒加载把首屏 JS 降到 420KB gzip；字体改 `font-display: swap` 并子集化。结果：LCP 到 2.1s，CLS 从 0.18 到 0.04（给图片写了宽高）。副作用是运营不能再把五张图都塞进首屏，产品接受「首屏只放一张」。没有前后数字的优化不算做完。

**B5. 技术调研：最近 6 个月学过并真正用进项目的一项技术。**

**标准答案示例：**  
把后台从 Webpack 5 迁到 Vite。调研维度：冷启动从 45s 降到约 3s、HMR 从 3～5s 到百毫秒级、生态上 `unplugin-auto-import` 和 Vue 插件成熟。风险是 Module Federation 和两个冷门 loader 不兼容，所以先迁日常开发、生产仍用 Vite 的 Rollup 打包并在预发对比产物体积。回退计划是保留 Webpack 配置一周双跑。后悔点是没先清掉 moment，迁移后体积几乎没降，后来才按需换成 dayjs。

---

## 1. 计算机基础（前端视角）

3–5 年岗位不会考操作系统填空，但会用基础解释「卡顿、内存涨、跨域、缓存」。

### 1.1 必掌握

- 进程 vs 线程；浏览器多进程（Browser / GPU / 多个 Renderer），JS 单线程与 Worker。
- 栈与堆：原始类型与引用类型；闭包为什么能拖住内存。
- 时间复杂度直觉：列表渲染、diff、查找为什么 O(n²) 会卡。
- HTTP/1.1 vs HTTP/2 vs HTTP/3：队头阻塞、多路复用、QUIC。
- 同源策略、Cookie / Token、CORS 预检。

### 1.2 面试题

**Q1. ★★ 浏览器为什么是多进程，JS 却是单线程？**

**标准答案：**  
浏览器采用多进程是为了稳定性和安全。典型模型是：一个 Browser 进程管用户界面和网络调度，一个 GPU 进程负责合成，每个标签页一个 Renderer 进程。某个页面崩溃或被恶意脚本攻击，不会拖垮整个浏览器；Renderer 还跑在沙箱里，限制文件系统等权限。

页面里的 JavaScript 仍然是单线程的：同一时刻只有一个调用栈在执行，这样操作 DOM 不会出现两个线程同时改同一节点的竞争。异步并不是多线程，而是事件循环：当前栈清空后，再取微任务、宏任务。计算密集型工作可以丢给 Web Worker，Worker 有自己的线程和全局，但不能直接碰 DOM，只能通过 `postMessage` 把结果传回主线程。

所以「多进程」解决的是浏览器产品级隔离，「单线程」解决的是 JS 与 DOM 的一致性。

**追问标准答：**  
主线程在忙：HTML/CSS 解析、JS 编译与执行、样式计算、布局（reflow）、绘制、处理输入事件。其中任何长任务（例如超过 50ms 的同步 JS）都会导致掉帧和输入延迟。

---

**Q2. ★★ 从输入 URL 到页面展示，发生了什么？**

**标准答案：**  
分网络和渲染两段。

网络：浏览器先看缓存（强缓存命中则不发请求；否则协商缓存，可能 304）。未命中则 DNS 解析得到 IP，建立 TCP，HTTPS 再做 TLS 握手，然后发 HTTP 请求。HTTP/2 上同一连接可多路复用；HTTP/1.1 则受连接数和队头阻塞限制。

渲染：响应体若是 HTML，字节流边下边解析。解析器遇到 `<script>` 且无 `async`/`defer` 会阻塞后续 HTML 解析，因为脚本可能 `document.write`。解析生成 DOM；CSS 不论放哪都会生成 CSSOM，且 CSS 会阻塞首次渲染。DOM + CSSOM 合成 Render Tree（`display:none` 不在树上，`visibility:hidden` 在）。接着 Layout 算几何，Paint 画图层，Composite 由 GPU 合成到屏幕。解析过程中还有 preload scanner 并行扫描后续的图片、CSS、JS，提前发请求。

**追问标准答：**  
关键渲染路径指「收到 HTML 到第一次绘制」必须完成的工作。优化手段是减少阻塞资源：CSS 放头且尽量短，JS 加 `defer`，关键图 `preload`。

---

**Q3. ★★ 解释一次跨域请求。什么时候会发 OPTIONS？**

**标准答案：**  
同源策略要求协议、域名、端口都相同，例如 `https://a.com` 与 `https://api.a.com`、`http://a.com`、`https://a.com:8080` 都不同源。前端用 `fetch`/`XHR` 读跨源响应时，浏览器会拦截 JS 读取，除非服务器返回 CORS 头：`Access-Control-Allow-Origin` 等。

CORS 把请求分成简单请求和非简单请求。简单请求限定为 GET/POST/HEAD，且头和 Content-Type 在白名单内（如 `text/plain`、`application/x-www-form-urlencoded`）。一旦出现自定义头（`Authorization`）、`Content-Type: application/json`，或 PUT/DELETE 等方法，就是非简单请求：浏览器先发 OPTIONS 预检，询问 `Access-Control-Allow-Methods`、`Allow-Headers`、`Max-Age`。预检通过才发真正的请求。

若带 Cookie（`credentials: 'include'`），`Allow-Origin` 必须是具体源而不能是 `*`，还要 `Access-Control-Allow-Credentials: true`。

**反直觉：** 跨域时服务端往往已经收到并处理了请求，浏览器拦截的是前端读响应。所以「CORS 报错」不等于请求没打到后端。

**追问标准答：**  
JSONP 只适合 GET 且有 XSS 风险，现代项目不用。反向代理（开发时 Vite proxy、生产时 Nginx 同域转发）是「让浏览器看起来同源」，不是 CORS 本身。

---

## 2. HTML5 与页面结构

考核点：语义化、可访问性、表单与多媒体、与 SEO / 小程序 / WebView 相关的结构意识。不是背标签列表。

### 2.1 知识清单

| 主题 | 要能讲清 |
|------|----------|
| 语义化 | `header`/`nav`/`main`/`article`/`section`/`aside`/`footer` 的使用边界 |
| 文档与 SEO | `title`、`meta description`、heading 层级、`alt`、Open Graph |
| 资源提示 | `defer`/`async`、`preload`/`prefetch`/`preconnect` |
| 表单 | 原生校验、`inputmode`、文件上传、`FormData` |
| 存储 | Cookie / localStorage / sessionStorage / IndexedDB 适用场景 |
| 安全相关标签 | `rel="noopener"`、CSP 概念、避免 `innerHTML` 拼用户输入 |

### 2.2 面试题

**Q4. ★ `defer` 和 `async` 的区别？**

**标准答案：**  
两者都会让脚本并行下载，下载本身不阻塞 HTML 解析，这是相对「普通 `<script>` 阻塞解析」的共同点。

区别在执行时机和顺序：

| | 执行时机 | 多脚本顺序 |
|--|----------|------------|
| `async` | 下载完立刻执行，可能插在解析中途 | 谁先下完谁先执行，与书写顺序无关 |
| `defer` | DOM 解析完成后、`DOMContentLoaded` 之前 | 保持书写顺序 |

因此：与 DOM 结构无关、无依赖的统计脚本可用 `async`；有依赖、需要等 DOM 的业务脚本用 `defer`。`type="module"` 的模块脚本默认按 defer 语义执行，且模块之间按依赖图求值。`defer` 对 `document.write` 无意义，因为解析已结束。

**追问标准答：**  
放在 `</body>` 前的普通脚本也能减轻阻塞，但不会并行下载得像 `defer` 那么早。现代项目由打包工具把入口打成 module 或带 defer 的 script。

---

**Q5. ★★ 语义化除了「好看」还有什么价值？**

**标准答案：**  
语义化的价值有四块。第一，无障碍：读屏软件靠 `nav`、`main`、`button`、标题层级理解结构，纯 `div` 需要额外 `role` 和 tabindex 才能等价。第二，SEO：搜索引擎用 heading 层级、`article` 等判断内容块。第三，可维护性：后来的人从标签就能看出「这是导航还是脚注」，减少注释。第四，默认行为：`button` 自带键盘激活和表单提交，`a` 自带右键打开新标签，自己用 div 点要重写一遍。

后台系统即使不在意 SEO，登录表单、按钮、标题、表格仍应语义化。不必把每个卡片都改成 `article`，核心是可交互控件和文档大纲正确。

**追问标准答：**  
`div` 搭后台可以，但按钮不要用 `div` + 点击；用 `button`。图标按钮要有 `aria-label`。

---

**Q6. ★★ localStorage 能存 Token 吗？**

**标准答案：**  
能存，但不推荐把访问令牌放在 `localStorage`。任何 XSS（第三方脚本、被注入的 Markdown、错误的 `innerHTML`）都能读 `localStorage` 并把 Token 发走。Cookie 若设置 `HttpOnly`，JS 读不到，XSS 偷 Token 的路径被堵上（仍可能借用户会话发请求，所以还要 CSRF 防护：`SameSite=Lax/Strict`、CSRF Token）。同时加 `Secure`（仅 HTTPS）和合理过期。

如果必须用本地存储（例如纯 SPA 无 Cookie 域方案），前提是 XSS 防护做到位：默认转义、CSP `script-src` 白名单、禁止运行时拼接脚本、依赖审计。Refresh Token 更敏感，应只存在 HttpOnly Cookie 或安全存储，前端内存只留短效 Access Token。

结论：没有银弹。威胁模型是 XSS 还是 CSRF，决定 Token 放哪。面试答「绝对不能」或「完全没问题」都不完整。

**追问标准答：**  
`sessionStorage` 关标签即清，仍防不住 XSS。IndexedDB 同样可被同源 JS 读取。

---

## 3. CSS3、布局与移动端适配

JD 明确要求 PC / 移动端兼容。CSS 是区分「能还原设计稿」与「只会调框架」的模块。

### 3.1 必须扎实的布局

**盒模型**

- `content-box` vs `border-box`；全局 `box-sizing: border-box` 的原因。
- `margin` 折叠：相邻兄弟、父子（无 BFC 时）。
- BFC 触发：`overflow` 非 `visible`、`flex/grid` 子项、`display: flow-root` 等。作用：清除浮动、阻止 margin 折叠、独立包含块。

**Flexbox（必问）**

- 主轴 / 交叉轴；`flex-grow` / `flex-shrink` / `flex-basis` 的计算直觉。
- `flex: 1` 等于 `1 1 0%`（常见写法），与 `1 1 auto` 不同。
- 常见坑：子项默认 `min-width: auto` 导致无法收缩；用 `min-width: 0` 修横向溢出。

**Grid**

- `fr`、`minmax`、`auto-fill` vs `auto-fit`。
- 二维布局（后台看板、商品宫格、 holygrail）用 Grid 更合适；一维流式用 Flex。

**定位与层叠**

- `relative` / `absolute` / `fixed` / `sticky` 包含块。
- 层叠上下文：`z-index` 只在同一上下文比较；`transform`/`opacity`/`filter` 会新建上下文。
- `sticky` 失效：祖先 `overflow` 不为 `visible`。

### 3.2 响应式与移动端

- 视口：`width=device-width, initial-scale=1`；禁止盲目 `user-scalable=no`（无障碍）。
- 适配方案对比：

| 方案 | 思路 | 适用 | 代价 |
|------|------|------|------|
| 媒体查询 | 断点改布局 | 管理后台、内容站 | 断点要设计参与 |
| `rem` + 根字号 | 按屏宽缩放 | 活动页、营销 H5 | 大屏会过大，需上限 |
| `vw` / `vmin` | 与视口成比例 | 简单海报页 | 文字过小/过大 |
| Flexible / postcss-px-to-viewport | 设计稿 px 转 vw | 移动端 C 端 | PC 与 Mobile 两套稿时要拆 |
| 两套布局 | Desktop / Mobile 独立结构 | 岗位职责中的高还原独立站 | 维护成本高，还原度最高 |

- 1px 边框：设备像素比 `dpr=2/3` 时 CSS 1px 偏粗。方案：`transform: scaleY(0.5)`、`0.5px`（部分 iOS）、伪元素 + scale、`hairline` 工具类。
- 安全区：`env(safe-area-inset-*)`，刘海屏底部横条。
- iOS 输入框聚焦被键盘顶起 / 页面放大：`font-size ≥ 16px` 可避免 iOS 自动缩放。
- 滚动：iOS 橡皮筋、`100vh` 含工具栏问题（用 `dvh` / `svh` 或 JS 测可视高度）。
- 点击延迟与点透：历史上 300ms；`touch-action`；遮罩消失后下层被点击（延迟解绑或 `pointer-events`）。

### 3.3 面试题

**Q7. ★★ 说一下 `flex: 1` 和为什么 flex 子元素会撑破容器。**

**标准答案：**  
`flex: 1` 是 `flex-grow: 1; flex-shrink: 1; flex-basis: 0%` 的简写。意思是：先按 basis 0 分配，再把容器剩余空间按 grow 分给各子项，空间不够时按 shrink 收缩。它和 `flex: 1 1 auto` 不同：`auto` 的 basis 是内容尺寸，多个 `flex: auto` 的子项会先按内容占位再分剩余空间，往往看起来「分不均」。

子项撑破容器的原因是默认 `min-width: auto`（横向 flex）或 `min-height: auto`（纵向）。收缩算法不能把子项收到比「内容最小尺寸」更小，于是长单词、不换行的表格、大图会把 flex 容器撑开出现横向滚动。

修复：

```css
.item {
  flex: 1;
  min-width: 0;      /* 允许小于内容宽度 */
  overflow: hidden;  /* 或 auto，决定溢出策略 */
}
```

**追问标准答：**  
`flex-basis: 0` 与 `0%` 在多数浏览器等价。百分比 basis 相对的是 flex 容器的内径。

---

**Q8. ★★ 水平垂直居中有哪些方式？各自限制？**

**标准答案：**  
常用五种，按场景选，不要背成唯一答案。

1. **Flex（首选）**：父元素 `display: flex; justify-content: center; align-items: center;`。子项可以未知宽高。缺点：改变父元素布局模型，嵌套 flex 时要注意变成 flex 容器后子项的 `min-width` 问题。
2. **Grid**：父元素 `display: grid; place-items: center;`。二维居中最干净。老旧浏览器需确认支持。
3. **绝对定位 + transform**：子项 `position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%);` 父项 `position: relative`。宽高未知可用。缺点：子项脱离文档流；`transform` 会创建层叠上下文，可能影响 `z-index` 和 `fixed` 包含块。
4. **绝对定位 + margin: auto**：子项定宽高，四边 `top/right/bottom/left: 0; margin: auto;`。未知宽高时无效。
5. **line-height**：只适合单行文字且高度固定，多行、图文混排会失效，不能当通用方案。

**追问标准答：**  
`text-align: center` 只水平居中行内内容；`vertical-align` 作用在行内盒或表格单元格，不是块级元素的垂直居中。

---

**Q9. ★★ 如何做移动端 1px 线？为什么会有这个问题？**

**标准答案：**  
CSS 的 `1px` 是 CSS 像素，不是设备物理像素。在 `devicePixelRatio = 2` 的屏上，1 CSS 像素对应 2×2 物理像素，设计师要的「1 物理像素细线」看起来就是 2 倍粗。

推荐实现（伪元素 + scale，四边和圆角都好用）：

```css
.hairline {
  position: relative;
}
.hairline::after {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  width: 200%;
  height: 200%;
  border: 1px solid #ddd;
  border-radius: 8px;
  transform: scale(0.5);
  transform-origin: 0 0;
  pointer-events: none;
  box-sizing: border-box;
}
```

`dpr=3` 时应用 `scale(1/3)`，可用 `window.devicePixelRatio` 或媒体查询 `@media (-webkit-min-device-pixel-ratio: 2)` 分档。

其他方案：`0.5px` 边框在部分 iOS 可用、安卓不一致；`transform: scaleY(0.5)` 只适合单边直线；`box-shadow: 0 0 0 0.5px` 兼容一般。

**追问标准答：**  
圆角在 scale 后要按 2 倍写 `border-radius`。不要用图片画 1px，会糊且难换色。

---

**Q10. ★★★ `z-index` 很大却盖不住？**

**标准答案：**  
`z-index` 只在同一个层叠上下文里比较。一旦祖先创建了新的层叠上下文，子元素无论 `z-index: 99999` 都无法越过祖先所在的那一层。

会创建层叠上下文的常见原因：`position` 非 static 且 `z-index` 非 auto；`opacity` 小于 1；`transform`、`filter`、`perspective`、`will-change` 为这些属性；`isolation: isolate`；某些 `flex/grid` 子项的 `z-index`。

典型踩坑：弹层写在页面中部，父级做了 `transform: translateZ(0)` 开 GPU 加速，结果 mask 盖不住页头；表格 `position: sticky` 的表头与行内下拉的 `z-index` 互抢；地图组件内部自己起了层叠上下文。

排查：从目标元素往上找第一个「奇怪的」`transform/opacity/z-index`，把弹层用 portal 挂到 `body` 下，或调整祖先的上下文而不是继续加大数字。

**追问标准答：**  
`position: fixed` 的包含块在祖先有 `transform` 时不再是视口，而是该祖先。这和层叠问题经常一起出现。

---

**Q11. ★ 选择器优先级？**

**标准答案：**  
按特异性从高到低：`!important` > 行内 style > ID > 类 / 属性选择器 / 伪类 > 元素 / 伪元素。比较时按 (内联, ID 数, 类数, 元素数) 从左到右，不是「10 个类大于 1 个 ID」。相同特异性看源码顺序，后写的覆盖先写的。

`!important` 会让特异性战争升级，只留给第三方覆盖或工具类（如 `.hidden { display: none !important }`），业务样式禁止滥用。降低战争的方法：BEM 保证类名唯一、CSS Modules / scoped 限制作用域、约定组件根类只控制自己。

**追问标准答：**  
`:not()`、`:is()`、`:where()` 的特异性不同：`:where()` 特异性为 0，适合写默认样式；`:is()` 取参数里最高的那个。

---

## 4. JavaScript 核心（ES6+）

这是基础盘。讲不清 this / 闭包 / 事件循环 / 原型，后面的框架原理会一起垮。

### 4.1 类型与比较

- `typeof` / `instanceof` / `Object.prototype.toString` 的适用边界。
- `==` 与 `===`；`NaN`、`Object.is`、`+0` 与 `-0`。
- 装箱：`'abc'.length` 的临时包装对象。
- 深拷贝：`JSON` 丢函数/`undefined`/`Symbol`、Date、循环引用；`structuredClone`；手写递归要处理循环。

**Q12. ★★ 手写浅拷贝 / 深拷贝要注意什么？**

**标准答案：**  
浅拷贝只复制第一层。`Object.assign({}, obj)`、`{...obj}`、`arr.slice()` 都是浅拷贝：嵌套对象仍共享引用，改 `copy.nested.x` 会改到原对象。

深拷贝要递归复制所有层。`JSON.parse(JSON.stringify(obj))` 不是万能方案：会丢掉 `undefined`、函数、`Symbol`、`bigint`；`Date` 变成字符串；`NaN`/`Infinity` 变成 `null`；遇循环引用直接抛错；不能处理 `Map`/`Set`/`RegExp`。浏览器可用 `structuredClone`（支持循环引用和大量内置类型，仍不能克隆函数）。

手写深拷贝必须：用 `WeakMap` 记录「原对象 → 副本」处理循环引用；按类型分支（Date、RegExp、Map、Set、Array、普通对象）；需要 symbol 键时用 `Reflect.ownKeys`。

**参考实现：**

```javascript
function deepClone(value, map = new WeakMap()) {
  if (value === null || typeof value !== "object") return value;
  if (value instanceof Date) return new Date(value);
  if (value instanceof RegExp) return new RegExp(value);
  if (map.has(value)) return map.get(value);

  if (value instanceof Map) {
    const result = new Map();
    map.set(value, result);
    value.forEach((v, k) => result.set(deepClone(k, map), deepClone(v, map)));
    return result;
  }
  if (value instanceof Set) {
    const result = new Set();
    map.set(value, result);
    value.forEach((v) => result.add(deepClone(v, map)));
    return result;
  }

  const result = Array.isArray(value) ? [] : {};
  map.set(value, result);
  Reflect.ownKeys(value).forEach((key) => {
    result[key] = deepClone(value[key], map);
  });
  return result;
}
```

**追问标准答：**  
拷贝函数没有通用语义（闭包绑了外部环境）。默认不保留原型，除非 `Object.create(Object.getPrototypeOf(value))`。

### 4.2 作用域、闭包、this、原型

**作用域**

- 词法作用域；`var` 函数作用域 + 变量提升；`let/const` 暂时性死区。
- 模块作用域（ESM）与全局对象隔离。

**闭包**

- 定义：函数引用了定义时的外部词法环境。
- 用途：工厂、柯里化、React `useEffect` 过期闭包、模块私有变量。
- 代价：引用链导致 GC 无法回收；事件监听 / 定时器未释放。

**this**

绑定优先级（由高到低，便于口述）：

1. `new` 绑定
2. 显式绑定 `call` / `apply` / `bind`
3. 隐式绑定（对象方法调用）
4. 默认绑定（严格模式 `undefined`）

箭头函数：**没有自己的 this / arguments / prototype**，不能 `new`。

**原型**

- `__proto__`（实例）vs `prototype`（函数）。
- 原型链查找；`class` 是语法糖：`constructor`、实例方法在原型、静态方法在函数自身。
- `extends` 的寄生组合继承本质（能提到 `super` 必须先调用即可加分）。

**Q13. ★★ 下面输出是什么？为什么？（经典变形）**

```javascript
for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
```

**标准答案：**  
输出 `3 3 3`。`var i` 是函数作用域（此处即全局或外层函数）的同一个绑定。`for` 的三次循环都把回调推入宏任务队列，这些回调在循环结束后才执行，那时 `i` 已经被加到 3。箭头函数捕获的是这个变量本身，不是每次迭代的快照。

**正确输出 `0 1 2` 的四种写法：**

```javascript
// 1. let：每次迭代创建新绑定
for (let i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}

// 2. IIFE 把当前 i 拷进形参
for (var i = 0; i < 3; i++) {
  (function (j) {
    setTimeout(() => console.log(j), 0);
  })(i);
}

// 3. setTimeout 第三参数
for (var i = 0; i < 3; i++) {
  setTimeout((j) => console.log(j), 0, i);
}

// 4. bind
for (var i = 0; i < 3; i++) {
  setTimeout(console.log.bind(null, i), 0);
}
```

**追问标准答：**  
`let` 在 `for` 头里每次迭代有新的词法环境，`setTimeout` 关闭的是那一次的 `i`。`const` 同样每轮新绑定，但 `i++` 不能写在 `const i` 上，所以循环变量用 `let`。

---

**Q14. ★★ `call` / `apply` / `bind` 区别？bind 能改 this 吗？**

**标准答案：**  
三者都用来指定函数运行时的 `this`。`fn.call(thisArg, a, b)` 立即执行，参数逐个传递。`fn.apply(thisArg, [a, b])` 立即执行，参数是数组（或类数组）。`fn.bind(thisArg, a)` **返回新函数**，并不马上执行；预设参数可以柯里化。

`bind` 是硬绑定：返回的函数内部已经把 `this` 锁死，再 `bound.call(other)` 或再 `bind` 一次，**改不了第一次绑定的 this**。例外：用 `new bound()` 时，`new` 绑定优先于 bind，`this` 指向新实例（ES 规范如此，业务里几乎不用 `new` 一个 bind 过的函数）。

箭头函数没有自己的 `this`，`call/apply/bind` 改不了箭头函数的 `this`。

**参考实现（面试口述级 bind）：**

```javascript
Function.prototype.myBind = function (thisArg, ...preset) {
  const fn = this;
  if (typeof fn !== "function") throw new TypeError("not a function");
  const bound = function (...args) {
    return fn.apply(
      new.target ? this : thisArg,
      preset.concat(args)
    );
  };
  bound.prototype = Object.create(fn.prototype);
  return bound;
};
```

**追问标准答：**  
严格模式下 `call(null)` 的 `this` 是 `null`；非严格模式会变成全局对象。不要依赖非严格行为。

---

**Q15. ★★ 用原型解释 `instanceof`。**

**标准答案：**  
`obj instanceof Ctor` 的判定是：沿着 `obj` 的原型链（`[[Prototype]]` / `__proto__`）往上走，如果某一环严格等于 `Ctor.prototype`，则为 true；走到 `null` 仍没有则为 false。所以 `[] instanceof Array`、`[] instanceof Object` 都是 true。

跨 iframe 时两边的 `Array` 不是同一个函数，`Ctor.prototype` 不是同一对象，`instanceof Array` 会失败，应使用 `Array.isArray`。函数可以自定义 `Symbol.hasInstance` 覆盖默认算法。

```javascript
function myInstanceof(obj, Ctor) {
  if (obj === null || (typeof obj !== "object" && typeof obj !== "function")) {
    return false;
  }
  let proto = Object.getPrototypeOf(obj);
  const prototype = Ctor.prototype;
  while (proto) {
    if (proto === prototype) return true;
    proto = Object.getPrototypeOf(proto);
  }
  return false;
}
```

**追问标准答：**  
`instanceof` 右边必须是函数。原始值（`'a' instanceof String`）是 false，因为原始值没有走装箱后的对象原型链。

### 4.3 ES6+ 必须能讲能用

| 特性 | 面试常问点 |
|------|------------|
| 解构、默认参数、rest/spread | 浅拷贝；默认参数在「缺省」时生效，`undefined` 会触发，`null` 不会 |
| 模板字符串 | 标签模板（styled-components 原理相关） |
| `Symbol` | 唯一属性键、内置协议（iterator、toPrimitive） |
| 迭代器 / `for...of` | 与 `for...in` 区别（后者枚举可枚举键，含原型） |
| Generator | 能说出「可暂停函数」即可，不必手写完整柯里化 |
| Proxy / Reflect | Vue3 响应式基础；能拦截 `in`、数组、属性删除 |
| Map / Set / WeakMap | WeakMap 弱引用做私有数据、Vue 依赖存储 |
| 可选链 / 空值合并 | `??` 只排除 `null/undefined`，不像 `\|\|` 把 `0` `''` 当假 |
| 模块 ESM | 静态结构、树摇、循环依赖的暂时性死区；与 CJS 互操作坑 |

**Q16. ★★ `??` 和 `||` 的区别？什么时候必须用 `??`？**

**标准答案：**  
`||` 看布尔真假：左边是假值（`false`、`0`、`''`、`NaN`、`null`、`undefined`）就取右边。`??` 只在左边是 `null` 或 `undefined` 时才取右边，`0`、`''`、`false` 都会保留。

必须用 `??` 的场景：表单数字允许 `0`（库存、页码）、开关允许 `false`、搜索框允许空字符串作为已输入状态、配置项「没传才用默认值」。

```javascript
const page = input.page || 1;   // 错误：page=0 会变成 1
const page = input.page ?? 1;   // 正确：0 仍保留
const title = name ?? "未命名"; // name 为 '' 时仍显示空；产品要兜底再用 ||
```

**追问标准答：**  
`?.` 与 `??` 常连用：`user?.profile?.age ?? 18`。`??` 不能直接和 `&&` / `||` 混写而不加括号。

---

**Q17. ★★ 为什么 Vue3 用 Proxy 而不是 `Object.defineProperty`？**

**标准答案：**  
Vue2 对对象每个已有属性调用 `Object.defineProperty` 劫持 getter/setter，初始化就要递归把嵌套对象都定义一遍。缺陷是：

1. 新增属性侦测不到，必须 `Vue.set` / `this.$set`。
2. 删除属性侦测不到，必须 `Vue.delete`。
3. 数组不能侦测索引赋值和改 `length`，只能劫持变异方法（`push`/`splice` 等）。
4. 对象很大时，启动阶段递归 define 成本高。

Vue3 用 `Proxy` 拦截对象的读/写/`in`/删除/数组索引等操作，代理的是「对象本身」而不是预先列出每个 key，因此新增、删除、数组索引都能触发更新。`Reflect` 配合作出默认行为并保证 `this` 正确。配合懒代理：只有被访问到的嵌套对象才再包一层 Proxy，启动更快。

代价：不支持 IE；Proxy 对象不能用 `===` 等于原对象，需要 `toRaw`；对 `Map`/`Set` 要单独处理。这是 Vue3 放弃 IE 后的明确取舍。

**追问标准答：**  
`ref` 用 `.value` 是因为 Proxy 不能代理原始类型，需要对象盒子。模板里自动解包是编译器/渲染器做的。

### 4.4 异步：Promise、async/await、事件循环

**事件循环模型（口述版）**

1. 执行一个宏任务（script / setTimeout 回调 / UI 事件 / I/O）。
2. 清空所有微任务（Promise `then`、`queueMicrotask`、MutationObserver、`await` 后续）。
3. 可能渲染。
4. 取下一个宏任务。

Node 与浏览器细节不同（Node 有 `process.nextTick`、旧版 stages），业务面试讲浏览器模型即可；不要混用「检查阶段」背诵除非岗位偏 Node。

**Promise**

- 三种状态，状态凝固。
- `then` 返回新 Promise；回调里抛错进入失败；返回 thenable 会展开。
- `Promise.all` 失败短路；`allSettled` 等全部；`race` 谁快用谁（含失败）；`any` 第一个成功。
- 手写题高频：简易 Promise、`all`、并发限制、retry、timeout。

**async/await**

- `await` 把后续放入微任务；等价于 `then`。
- 错误用 `try/catch`；不要在循环里无脑 `await`（串行），该 `Promise.all` 就并行。
- 过期请求：后发先至，用序号 / `AbortController`。

**Q18. ★★ 说出下面打印顺序**

```javascript
console.log(1);
setTimeout(() => console.log(2), 0);
Promise.resolve().then(() => console.log(3));
async function f() {
  console.log(4);
  await Promise.resolve();
  console.log(5);
}
f();
console.log(6);
```

**标准答案：**  
打印顺序是 **`1 4 6 3 5 2`**。

逐步：

1. 同步执行 `console.log(1)`，输出 1。
2. `setTimeout` 把「输出 2」放入宏任务队列。
3. `Promise.resolve().then(...)` 把「输出 3」放入微任务队列。
4. 调用 `f()`：`async` 函数在遇到 `await` 之前是同步的，输出 4。`await Promise.resolve()` 把后续 `console.log(5)` 作为微任务挂上。
5. `f()` 返回一个 Promise，调用方没有 await，继续同步执行，输出 6。
6. 同步栈清空，先清空微任务：先输出 3，再输出 5。
7. 取下一个宏任务，输出 2。

结论：`async` 函数 `await` 之前是同步的；`await` 之后是微任务，早于 `setTimeout(0)`。

**追问标准答：**  
若把 `await Promise.resolve()` 改成 `await 1`，现代引擎仍会走 Promise 包装，`5` 仍然在微任务里。`queueMicrotask` 与 `then` 同级。

---

**Q19. ★★ 如何控制并发（例如最多 3 个上传）？**

**标准答案：**  
维护「进行中集合 + 上限」。每启动一个任务就放入集合；当集合大小达到 `limit` 时 `await Promise.race(executing)`，等任意一个完成再启动下一个。全部启动后 `Promise.all` 按入参顺序汇总。必须处理：单个失败默认让 `Promise.all` 失败，若要「失败也继续」则每个任务自己 catch 或改用 `allSettled`；取消用 `AbortController` 传给 iterator。

**参考实现：**

```javascript
async function asyncPool(limit, items, iteratorFn) {
  const ret = [];
  const executing = new Set();

  for (const [index, item] of items.entries()) {
    const p = Promise.resolve().then(() => iteratorFn(item, index));
    ret.push(p);
    executing.add(p);
    const clean = () => executing.delete(p);
    p.then(clean, clean);

    if (executing.size >= limit) {
      await Promise.race(executing);
    }
  }
  return Promise.all(ret);
}

await asyncPool(3, files, (file) => upload(file));
```

**追问标准答：**  
`Promise.all` 是全部一起开始，没有上限。超时用 `Promise.race([job, delayReject(ms)])`。

---

**Q20. ★★★ 请求竞态怎么处理？**

**标准答案：**  
竞态指「先发的请求后返回」，用旧数据覆盖新数据。例如搜索框连打字：请求 A（「关」）后发出请求 B（「关键」），A 后到，列表被刷回错误结果。

三种做法可组合：

1. **序号忽略过期响应**：每次请求 `++seq`，回调里若 `seq !== latest` 则丢弃。实现简单，服务端仍会算完。
2. **AbortController 取消上一次**：新请求发出时 `abort()` 上一次；`catch` 里识别 `error.name === 'AbortError'`，不当成业务失败。
3. **产品层防抖**：搜索用 debounce 300ms 减少请求数，再加 1 或 2。滚动加载用节流，语义不同。

```javascript
let seq = 0;
async function search(q) {
  const current = ++seq;
  const data = await api.search(q);
  if (current !== seq) return;
  render(data);
}

let controller = null;
async function searchAbort(q) {
  controller?.abort();
  controller = new AbortController();
  const res = await fetch(`/search?q=${encodeURIComponent(q)}`, {
    signal: controller.signal,
  });
  render(await res.json());
}
```

**追问标准答：**  
React Query / SWR 用 queryKey 丢掉过期结果。RxJS `switchMap` 等价于「新订阅取消旧订阅」。

### 4.5 函数与性能细节

- 防抖 / 节流：搜索框 vs 滚动监听 vs resize。trailing / leading。
- 柯里化、偏函数：面试可一笔带过，重点是闭包。
- 记忆化：缓存纯函数；注意缓存键与内存。
- 尾递归：JS 引擎基本不保证 TCO，别当优化手段吹。

**Q21. ★ 手写 debounce / throttle。**

**标准答案：**  
防抖（debounce）：连续触发时只认「停下来之后」那一次。用于搜索建议、resize 结束再算布局、按钮防连点（也可 leading）。  
节流（throttle）：固定时间窗口内最多执行一次。用于滚动、mousemove、高频上报。要能说清 leading（立即执行第一次）和 trailing（窗口结束补一次）。

React 里若把 `debounce` 写在函数组件体内，每次 render 会新建函数、定时器失效。应 `useMemo`/`useRef` 保存实例，卸载时 `clearTimeout`。

**参考实现：**

```javascript
function debounce(fn, wait, { leading = false } = {}) {
  let timer = null;
  let invoked = false;
  const debounced = function (...args) {
    const ctx = this;
    if (leading && !timer && !invoked) {
      invoked = true;
      fn.apply(ctx, args);
    }
    clearTimeout(timer);
    timer = setTimeout(() => {
      timer = null;
      invoked = false;
      if (!leading) fn.apply(ctx, args);
    }, wait);
  };
  debounced.cancel = () => {
    clearTimeout(timer);
    timer = null;
    invoked = false;
  };
  return debounced;
}

function throttle(fn, wait, { leading = true, trailing = true } = {}) {
  let last = 0;
  let timer = null;
  return function (...args) {
    const now = Date.now();
    const ctx = this;
    if (!last && !leading) last = now;
    const remaining = wait - (now - last);
    if (remaining <= 0) {
      clearTimeout(timer);
      timer = null;
      last = now;
      fn.apply(ctx, args);
    } else if (trailing && !timer) {
      timer = setTimeout(() => {
        last = leading ? Date.now() : 0;
        timer = null;
        fn.apply(ctx, args);
      }, remaining);
    }
  };
}
```

**追问标准答：**  
搜索框用 debounce；无限滚动用 throttle。两者都能解决「次数太多」，但用户体验不同：debounce 会等用户停手，throttle 会边滑边更新。

---

## 5. 浏览器、网络、渲染与兼容

### 5.1 渲染流水线（性能优化的前置课）

1. 解析 HTML → DOM
2. 解析 CSS → CSSOM（CSS 会阻塞渲染）
3. 合成 Render Tree（不包含 `display:none`）
4. Layout（reflow）
5. Paint（repaint）
6. Composite（合成层、GPU）

**触发重排的典型操作**：改几何、读布局属性（`offsetHeight`、`getComputedStyle`）穿插写入导致强制同步布局。优化：读写分离、`transform`/`opacity` 做动画、`will-change` 慎用。

**Q22. ★★ 重排、重绘、合成的区别？为什么用 transform 做动画？**

**标准答案：**  
重排（reflow / layout）：元素几何变化（宽高、位置、display、字体大小、窗口 resize）导致重新计算布局，成本最高，可能影响整棵子树。  
重绘（repaint）：几何不变但外观变（颜色、背景、outline），需要重新栅格化，比重排便宜。  
合成（composite）：已画好的图层由 GPU 按位置叠在一起。`transform` 和 `opacity` 的变化通常只走合成，不触发 layout/paint（前提是该元素已提升为合成层）。

因此位移动画用 `transform: translate`，淡入用 `opacity`，不要用 `left/top` 或改 `margin`。`will-change: transform` 可以提前提升图层，但图层占内存，动画结束应去掉，不能给一堆元素乱加。

读写交错会强制同步布局：先 `el.style.width = '100px'` 再读 `el.offsetHeight`，浏览器必须立刻重排才能给出正确值。优化是批量写再批量读。

**追问标准答：**  
`display: none` 到显示会重排；`visibility: hidden` 仍占布局只重绘。

### 5.2 缓存与加载

| 机制 | 要点 |
|------|------|
| 强缓存 | `Cache-Control: max-age` / `Expires` |
| 协商缓存 | `ETag` / `If-None-Match`，`Last-Modified` / `If-Modified-Since` |
| 文件名 hash | Webpack/Vite 内容哈希，HTML 不长期强缓存，静态资源长期缓存 |
| Service Worker | 离线、自定义缓存策略；调试成本高 |
| CDN | 就近、回源、缓存键（query、cookie） |

**Q23. ★★ 发版后用户还看到旧页面？**

**标准答案：**  
根因几乎都是缓存策略配错，按这条查：

1. **HTML 被强缓存**：`index.html` 若 `Cache-Control: max-age=31536000`，用户会一直拿旧 HTML，里面引用的还是旧的 `app.abc123.js`。正确做法：HTML 用 `Cache-Control: no-cache`（每次协商）或不缓存；带 hash 的 JS/CSS 才 `max-age=31536000, immutable`。
2. **CDN 未刷新**：源站更新了，边缘节点还是旧文件。发版要刷新 HTML 的 CDN，或 HTML 不走长缓存。
3. **Service Worker**：SW 可能继续 serve 旧壳。发版要更新 SW 版本，客户端 `skipWaiting` + `clients.claim`，或提示用户刷新。
4. **chunk 404**：新 HTML 引用新 hash，但用户开着旧页面动态 import 已下线的旧 chunk。解决：发布时保留最近 1～2 个版本的静态资源，或对 chunk 404 做整页刷新。

**标准发布策略：** 文件名内容哈希 + HTML 短缓存 + 保留旧 chunk 一段时间 + 必要时 SW 更新提示。

**追问标准答：**  
用 `contenthash` 而不是整包 `hash`，避免无关文件变更导致缓存全失效。`?v=时间戳` 能破缓存，但不如文件名哈希干净。

### 5.3 安全（前端能扛的部分）

- XSS：存储型 / 反射型 / DOM 型；转义、CSP、避免 `dangerouslySetInnerHTML`。
- CSRF：SameSite Cookie、CSRF Token、关键操作二次确认。
- 开放重定向、第三方脚本（GTM）权限。
- 依赖供应链：lockfile、审计。

**Q24. ★★ 如何防止 XSS？**

**标准答案：**  
XSS 是把攻击者的脚本注入到页面，在用户浏览器里执行。分存储型（写入数据库再读出）、反射型（URL 参数立刻反射到页面）、DOM 型（纯前端用 `location.hash` 拼进 `innerHTML`）。

防护分层：

1. **默认转义**：Vue `{{ }}`、React JSX 文本节点会把 `<` 编成实体。危险的是 `v-html`、`dangerouslySetInnerHTML`、直接 `innerHTML`、`document.write`。
2. **富文本**：用 DOMPurify 等白名单消毒，只留安全标签和属性，去掉 `onerror`、`javascript:`。
3. **URL**：用户提供的链接校验协议，禁止 `javascript:` 和 `data:text/html`。
4. **CSP**：`Content-Security-Policy: script-src 'self'`，禁止行内脚本和未知源（需避免滥用 `unsafe-inline`）。
5. **Cookie**：会话用 `HttpOnly`，降低偷 Token 的收益。

只答「用 innerText」不完整：还有属性注入、第三方 widget、JSON 里的 `</script>`。

**追问标准答：**  
JSON 塞进 `<script>` 要把 `<` 替换为 `\u003c`。Markdown 渲染必须消毒。CSRF 是另一类问题：盗用已登录会话发请求，靠 SameSite Cookie 和 CSRF Token，不是转义能解决的。

### 5.4 跨浏览器 / 跨设备兼容（JD 重点）

**策略，而不是背兼容表**

1. 明确浏览器矩阵（Chrome 近两年版、Safari iOS、微信内置 X5、少量 Android WebView）。
2. 构建层：Browserslist + Autoprefixer + Babel / SWC 按目标转译。
3. 能力检测，不靠 UA 硬分支（`IntersectionObserver` 等）。
4. 渐进增强：核心路径可用，增强动画 / Backdrop。
5. 真机与云测：iOS 滚动、微信 JS-SDK、键盘、1px、字体。
6. Polyfill 策略：按需（core-js + 使用点）避免全量。

**PC vs Mobile 常见差异**

- 事件：hover 在触摸设备会「粘住」；点击热区 ≥ 44px。
- 布局：两套设计稿 vs 响应式；导航从顶栏变底栏。
- 性能：中低端安卓 JS 预算更紧，图片与列表必须虚拟化或分页。
- 字体与溢出：英文断词、中文连续字符串、`word-break`。
- 兼容 API：`position: sticky`、`gap` in flex（旧 Safari）、`aspect-ratio`、`:has()`。

**Q25. ★★ 你如何系统解决兼容问题，而不是「出了再修」？**

**标准答案：**  
兼容要前置成工程策略，步骤如下。

1. **浏览器矩阵**：例如 Chrome 近两个大版本、Safari iOS 15+、微信内置内核、覆盖 95% 流量的 Android WebView。矩阵写进 README，超出范围的 bug 降级而不是无限修。
2. **构建对齐矩阵**：Browserslist 同一份配置驱动 Autoprefixer 和 Babel/SWC。语法转译和 API polyfill 分开（见 Q26）。
3. **能力检测不靠 UA**：`if ('IntersectionObserver' in window)`，缺了再 polyfill 或降级成滚动监听。
4. **渐进增强**：下单、登录等核心路径用稳定 API；模糊背景、`:has()` 只做增强。
5. **真机抽样**：iOS 键盘、`100vh`、1px、微信 JS-SDK、中低端安卓列表。实验室 Chrome 过了不等于微信里过了。
6. **回归**：关键页 Lighthouse 移动端、有条件上视觉回归。出过的坑收成工具函数（`safeArea`、`hairline`、`isIOS` 仅作最后手段）。

案例：iOS 日期 `new Date('2024-01-01 12:00:00')` 为 Invalid Date，统一走 `YYYY-MM-DDTHH:mm:ss` 或手动拆分；微信里字体小于 12px 会被重置，设计稿要守底线。

**追问标准答：**  
`user-scalable=no` 会伤害无障碍，不要为了「更像 App」默认关掉缩放，除非产品明确要求并做了替代放大。

---

**Q26. ★★ CSS 兼容怎么做？JS 兼容怎么做？**

**标准答案：**  
两套机制，不要混成「都靠 Babel」。

**CSS：** Autoprefixer 根据 Browserslist 加 `-webkit-` 等前缀。新特性用 `@supports (display: grid)` 写降级（不支持就走 flex）。不要把 `:has()`、`aspect-ratio` 当唯一实现，要有旧写法。某些 flex `gap` 在旧 Safari 无效，用 margin 兜底。

**JS：**  
- **语法转译**（Babel / SWC / esbuild）：把可选链、箭头函数、`class` 变成目标环境能解析的语法。不转译，旧浏览器会直接 SyntaxError，整页白屏。  
- **API polyfill**（core-js、polyfill.io）：`Promise`、`fetch`、`Array.includes` 是运行时对象，只转语法不够。按需注入（`useBuiltIns: 'usage'`）避免把 IE 的垫片打进现代浏览器包。

所以：语法错误靠转译；`xxx is not a function` 靠 polyfill。这是加分点。

**追问标准答：**  
TypeScript 只擦类型，不负责降级。`target` 配太低会让包变大，应与 Browserslist 一致。

---

## 6. 框架：Vue / React / Angular

JD：**至少一门精通，熟悉生态，理解核心原理。**  
面试策略：主框架挖到底，另一门做对比题。

### 6.1 虚拟 DOM 与更新机制（三框架共通）

**为什么需要虚拟 DOM**

- 直接操作 DOM 成本高（布局、绘制）。
- 用 JS 对象描述 UI，diff 后打补丁。
- 额外价值：跨平台（RN、小程序）、声明式、批量更新。

**不是银弹**：节点极少时直接操作可能更快；虚拟 DOM 的价值是**可预测的更新模型 + 工程化**，不是「一定更快」。

**Diff 直觉（面试够用）**

- 同层比较，不跨层移动（Vue/React 经典启发式）。
- `key` 的作用：复用节点身份；列表用 index 当 key 会在乱序/插入时错位（输入框状态错绑）。
- React 早期递归 diff；现在 Fiber 可中断。Vue3 编译期静态提升 + PatchFlag，运行时 diff 更针对性。

**Q27. ★★ 为什么列表必须写 key？index 当 key 什么时候可以？**

**标准答案：**  
虚拟 DOM 做 diff 时默认「同层按顺序比」。没有稳定身份时，中间插入一项会被当成「后面每一项都变了」，导致 DOM 复用错位：输入框的值、组件内部 state、动画状态会串到错误的那一行。`key` 告诉算法「这是同一个逻辑项」，可以移动复用而不是销毁重建。

`index` 当 key **仅当列表静态**：不插入、不删除、不排序、项没有局部状态（如输入框）。一旦 `unshift`、过滤、拖拽排序，index 会变，等于没写。正确做法是用业务 id（数据库 id、skuId）。禁止用随机数当 key，每次 render 都是新 key，反而强制全部重建。

**追问标准答：**  
Vue 的 `v-for` 和 React 的 `map` 规则相同。key 要在列表那一层唯一，不必全局唯一。

### 6.2 Vue 核心原理

**Vue2 响应式**

- `Object.defineProperty` 劫持 getter/setter。
- 依赖收集：Watcher 订阅属性。
- 数组：重写变异方法；索引直接赋值、`length` 需 `Vue.set`。
- 对象新属性需要 `Vue.set` / `this.$set`。

**Vue3 响应式**

- `Proxy` 代理整对象；`Reflect` 保证 this 正确。
- `reactive` 深代理；`ref` 基本类型盒子；`toRefs` 保持响应式解构。
- `effect` / 依赖桶：`WeakMap(target) → Map(key) → Set(effect)`。
- 避免：`reactive` 解构丢失响应式；对 `ref` 在模板自动解包、在 script 要 `.value`。

**编译优化（能讲到即加分）**

- 静态节点提升、PatchFlag、Block Tree、`cacheHandlers`。
- Composition API 更好做逻辑复用（hooks 风格），Options 仍可用。

**生态**

| 主题 | 要点 |
|------|------|
| Vue Router | 前端路由 history vs hash；导航守卫；路由懒加载 `() => import(...)` |
| Vuex | 单向数据流；mutation 同步；action 异步；模块命名空间 |
| Pinia | 无 mutation 强制；TS 友好；去掉 modules 嵌套心智 |
| 组件通信 | props/emit、provide/inject、v-model 原理（value + input / modelValue + update） |

**Q28. ★★ Vue2 和 Vue3 响应式差异？**

**标准答案：**  
核心差异是「怎么劫持」以及「能侦测什么」。

Vue2：初始化时递归 `Object.defineProperty`，给每个已有 key 加 getter/setter。读取时收集依赖（Watcher 订阅该属性），写入时通知 Watcher 更新视图。数组通过重写 `push`/`pop`/`splice` 等 7 个变异方法。侦测不到：对象新属性、删除属性、`arr[i] = x`、改 `arr.length`。要用 `Vue.set`/`Vue.delete`。大对象启动就要递归 define，成本高。

Vue3：`reactive` 用 `Proxy` 代理整个对象，`get` 里 `track`，`set`/`deleteProperty` 里 `trigger`。依赖桶结构是 `WeakMap(target) → Map(key) → Set(effect)`。新增、删除、数组索引都能触发。嵌套对象懒代理：访问到才包 Proxy。`ref` 给原始值做盒子。配合编译器：静态节点提升、PatchFlag，更新时不必全量 diff 静态子树。

口述时画这张图即可：

```
effect 执行 → 读 obj.x → track(obj, 'x')
set obj.x  → trigger(obj, 'x') → 重跑相关 effect → 组件 update
```

**追问标准答：**  
`reactive` 解构会丢响应式，要用 `toRefs`。`ref` 在 script 里必须 `.value`，模板自动解包。`shallowRef` 只代理 `.value` 这一层，适合大而少变的对象。

---

**Q29. ★★ `v-if` 和 `v-show`？和 `v-for` 一起用的顺序？**

**标准答案：**  
`v-if` 为 false 时不渲染该分支，条件为真才创建，为假则销毁（连同子组件状态）。适合很少切换或默认不出现的权限块、弹层内容。  
`v-show` 始终渲染 DOM，用 `display: none` 切换。适合频繁切换的 tab，避免反复初始化。隐藏时子组件仍会挂载，只是不可见。

不要同时在同一元素上写 `v-if` 和 `v-for`。Vue2 中 `v-for` 优先级更高，容易先循环再判断。Vue3 中 `v-if` 优先级更高，`v-if` 看不到 `v-for` 的作用域变量。标准写法：用计算属性先过滤列表，再 `v-for`；或外层 `template` 包一层。

**追问标准答：**  
`v-if` / `v-else-if` / `v-else` 必须相邻。`key` 写在 `v-if` 分支上可强制切换时销毁重建（例如切用户后重置表单）。

---

**Q30. ★★ nextTick 是什么？**

**标准答案：**  
Vue 对同一事件循环里的多次数据改动做异步批量更新：先把组件标为 dirty，等当前同步代码结束，在微任务里才真正 patch DOM。所以你在 `this.count++` 的下一行去读 `this.$el.textContent` 往往还是旧的。`nextTick(cb)` 把回调放到 DOM 更新之后执行，默认用 `Promise.then`（微任务），不支持 Promise 时降级到 `MutationObserver` 或 `setTimeout`。

```javascript
count.value++;
await nextTick();
inputRef.value.focus(); // 此时输入框已在文档里
```

批量更新的原因：一次点击里改 10 个字段只渲染一次，而不是 10 次。

**追问标准答：**  
`setTimeout(0)` 也能「稍后」，但它是宏任务，可能已经夹了一次绘制，比 `nextTick` 晚，不应用来等 Vue 的 DOM。

---

**Q31. ★★★ 如何实现一个迷你响应式？（口述或白板）**

**标准答案：**  
最小模型三件套：当前活跃的 effect、`track` 收集、`trigger` 触发，再用 Proxy 包对象。分支切换（如三元从 `ok` 为 true 改 false）要在每次 effect 重跑前清理旧依赖，否则会「改已经不读的属性还触发更新」。

**参考实现：**

```javascript
let activeEffect = null;
const bucket = new WeakMap(); // target -> Map(key -> Set(effect))

function effect(fn) {
  const wrapped = () => {
    cleanup(wrapped);
    activeEffect = wrapped;
    fn();
    activeEffect = null;
  };
  wrapped.deps = [];
  wrapped();
  return wrapped;
}

function cleanup(effectFn) {
  effectFn.deps.forEach((dep) => dep.delete(effectFn));
  effectFn.deps.length = 0;
}

function track(target, key) {
  if (!activeEffect) return;
  let depsMap = bucket.get(target);
  if (!depsMap) bucket.set(target, (depsMap = new Map()));
  let dep = depsMap.get(key);
  if (!dep) depsMap.set(key, (dep = new Set()));
  dep.add(activeEffect);
  activeEffect.deps.push(dep);
}

function trigger(target, key) {
  const depsMap = bucket.get(target);
  const dep = depsMap?.get(key);
  if (!dep) return;
  [...dep].forEach((fn) => fn());
}

function reactive(obj) {
  return new Proxy(obj, {
    get(target, key, receiver) {
      const val = Reflect.get(target, key, receiver);
      track(target, key);
      return val && typeof val === "object" ? reactive(val) : val;
    },
    set(target, key, value, receiver) {
      const ok = Reflect.set(target, key, value, receiver);
      trigger(target, key);
      return ok;
    },
  });
}

const state = reactive({ ok: true, text: "hello" });
effect(() => {
  console.log(state.ok ? state.text : "off");
});
state.text = "world";
```

**追问标准答：**  
`[...dep]` 避免 `trigger` 时 effect 先 cleanup 再收集导致 Set 遍历异常。嵌套 effect 要用栈保存 `activeEffect`。调度器可把触发改成异步批量化，对应 Vue 的 `queueJob`。

### 6.3 React 核心原理

**JSX**

- 语法糖 `React.createElement`；本质是描述 UI 的数据。
- 列表 key；条件渲染用三元，不要用 `&&` 对 `0`（会打出 0）。

**调和与 Fiber**

- Fiber：把更新拆成可中断工作单元，解决递归 diff 长时间占主线程。
- 优先级 / Lane；时间切片（概念层即可）。
- 双缓冲：workInProgress 与 current 树切换。

**Hooks 规则与原理直觉**

- 只在顶层调用：用链表按调用顺序对应 state。条件调用会错位。
- `useState`：队列更新，可能自动批处理（React 18 处处 batch）。
- `useEffect`：绘制后异步；依赖数组；清理函数（订阅、定时器）。
- `useLayoutEffect`：绘制前同步，防闪烁，阻塞绘制，少用。
- `useMemo` / `useCallback`：依赖变才换引用；不是免费的，先测再加。
- `useRef`：可变盒子，改 `.current` 不触发渲染。

**闭包陷阱**

- effect 捕获旧 state；用函数式更新 `setS(s => s+1)` 或把最新值放 ref。

**状态管理**

| 方案 | 何时用 |
|------|--------|
| 组件 state + 提升 | 局部 |
| Context | 低频率（主题、locale）；高频会 rerender 风暴 |
| Redux / Toolkit | 可预测、中间件、时间旅行、大团队规范 |
| MobX | 可变数据、观察细粒度；心智接近 Vue |
| Zustand / Jotai 等 | 轻量；讲清为何替换 Redux |

**React Router**：loader/action（新数据路由）知道即可；嵌套路由、懒加载 `React.lazy` + `Suspense`。

**Q32. ★★ Class 组件和函数组件 + Hooks 的本质差异？**

**标准答案：**  
Class 组件有长期存在的实例：`this.props` / `this.state` 在实例上，事件回调里读到的永远是「当前最新」的那一份（render 时写到 this 上）。函数组件每次 render 都是一次全新的函数调用，这次调用里的 `props`、`state`、闭包是一张快照。所以常见坑是：`setTimeout` 或未写对依赖的 `useEffect` 里拿到的是旧 state。

```javascript
function Counter() {
  const [n, setN] = useState(0);
  const onClick = () => {
    setTimeout(() => console.log(n), 1000); // 点三次仍可能打印 0
  };
  // 修复：setN(x => x + 1) 函数式更新，或用 useRef 存最新 n
}
```

Hooks 用链表按**调用顺序**把 state 挂到 Fiber 上，所以不能写在 if/循环里，否则顺序错位。Class 没有这个限制，但逻辑复用要靠 HOC / render props，嵌套和 this 绑定更烦。

**追问标准答：**  
`useEffect` 模拟 `componentDidMount` 时依赖写 `[]`，闭包仍捕获首次 props。需要最新值就加依赖，或 ref。

---

**Q33. ★★ useEffect 依赖漏了会怎样？多写了会怎样？**

**标准答案：**  
依赖数组告诉 React：「这些值变了才重新跑 effect；没变就跳过。」

漏依赖：effect 闭包一直用旧值。典型是订阅了 `userId` 对应的 websocket，但依赖是 `[]`，切用户后仍监听上一个人。eslint 的 `react-hooks/exhaustive-deps` 就是拦这个。

多依赖 / 依赖不稳定：每次 render 都是新对象或新函数（`options={{}}`、`onChange={() => {}}`），effect 每次都清理再执行，轻则重复请求，重则死循环（effect 里 `setState` 又产生新对象）。修复：把对象拆成原始值、用 `useMemo`/`useCallback` 稳定引用。

`useLayoutEffect` 依赖规则相同，只是执行时机在绘制前同步，会阻塞 paint，仅用于测量 DOM 防闪烁。

**追问标准答：**  
`useEffect` 在浏览器绘制**之后**异步执行。服务端没有 layout effect。StrictMode 开发环境双调用 effect 是为了暴露不纯副作用，生产只跑一次。

---

**Q34. ★★ React 18 批处理、并发、Suspense 你怎么理解？**

**标准答案：**  
**自动批处理：** React 18 在任何地方的多处 `setState`（含 Promise、setTimeout）都会合并成一次渲染。以前只在事件处理器里批处理。可用 `flushSync` 强制同步刷（几乎用不到）。

**并发：** 不是 JS 多线程。React 把渲染工作拆成可中断的小块（Fiber），高优先级更新（输入框）可以打断正在进行的低优先级渲染（大列表过滤），避免卡输入。`useTransition` 把「可延迟的状态」标成过渡更新；`useDeferredValue` 推迟昂贵派生值。

**Suspense：** 是「数据或代码还没就绪时展示 fallback」的协调机制，配合 `React.lazy` 和框架层数据请求。它不是 `async setState`。

**追问标准答：**  
`startTransition` 里的更新可以被打断；紧急更新（用户输入）不行。

---

**Q35. ★★★ 如何减少 rerender？**

**标准答案：**  
先用 Profiler 确认是谁在渲染、为什么渲染，再优化。常见手段：

1. **`React.memo` 包纯展示组件**，props 浅比较相同则跳过。
2. **稳定 props 引用**：父组件用 `useCallback`/`useMemo`，否则 memo 失效。
3. **状态下沉**：把高频 `setState`（输入框）放到叶子组件，避免整页跟着变。
4. **状态拆分**：把无关的 `theme` 和 `list` 拆开，或 Context 拆成「数据 Context」和「dispatch Context」。
5. **列表虚拟化**：看不见的行不要进 React 树。
6. **计算缓存**：昂贵派生数据 `useMemo`，先证明贵再加。

错误做法：给每个组件都包 memo；把所有对象都 useMemo。Context 值是新对象时，所有消费者都会渲染，这是后台卡顿的常见原因。

**追问标准答：**  
`useMemo` 不能保证永不重算，只是「依赖不变时跳过」。不要用它当副作用。

### 6.4 Angular 要点（主技术栈才深挖）

- 模块 / 组件 / 指令 / 服务；依赖注入层级（root / component）。
- 变更检测：Zone.js 默认；`OnPush` 靠输入引用变化。
- RxJS：冷热 observable、反压、`switchMap` 取消上一次请求（对比 React 竞态）。
- 路由守卫、懒加载模块。

**Q36. ★★（对比）Vue 的依赖收集和 React 的 setState 全量调和有何不同？**

**标准答案：**  
Vue 是「读时收集、写时精确通知」。组件 render / effect 过程中访问了哪些响应式属性，就把当前组件订阅上去；改 `state.a` 只通知订阅了 `a` 的那些 effect。Vue3 更新粒度仍以组件为界，但编译器 PatchFlag 让组件内部 patch 更少。心智是可变数据 + 自动追踪。

React 是「状态变了，从该组件开始默认往下调和」。`setState` 不记录你读了哪个字段。子组件默认也会 render，除非 `memo` 截断或 props 不变。心智是不可变快照 + 显式更新。

没有绝对更快。Vue 省去很多无用子树更新，但 Proxy 和依赖管理有成本；React 更新路径简单可打断（并发），大列表要自己 memo / 虚拟化。团队熟悉度和生态往往比微基准更重要。

**追问标准答：**  
MobX 更接近 Vue 的细粒度；Redux 更接近「显式 dispatch + 订阅」。Solid.js 把细粒度做到 DOM 节点级，没有虚拟 DOM。

---

### 6.5 框架通用工程问题

**Q37. ★★ 组件如何拆？如何避免 prop drilling？**

**标准答案：**  
按职责和变更频率拆，不按文件行数拆。一个组件只做一件事：展示、表单、请求容器分开。高频变化的（输入框）和低频的（页头）不要放在同一 state 里。组合优于继承：用 children / slots 插内容，不要多层 extends。

避免 prop drilling（层层传 props）的手段，按重量选：

1. 中间层不需要的数据不要传，把组件树压扁，或把消费方移到更近的位置。
2. Vue `provide/inject`、React Context：适合主题、i18n、当前用户。高频数据（每键一敲）不要塞进巨大 Context。
3. 轻量 store：Pinia、Zustand。跨页、跨深层树、需要 DevTools 时用。
4. 不要上来就上 Redux。「三个组件要用同一个 flag」用提升 state 就够。

**追问标准答：**  
服务端状态（列表、详情）和客户端状态（modal 开没开）分开。前者用 React Query / SWR，后者用本地 state。混在一个 Redux 里是常见过度设计。

---

**Q38. ★★ 受控 vs 非受控组件？**

**标准答案：**  
受控：表单值存在框架状态里，输入时 `onChange` / `@input` 更新状态，再回流到 `value`。好处是校验、禁用提交、格式化（只允许数字）都好做。代价是每个按键一次渲染。

非受控：值活在 DOM 里，提交时用 ref 读取。适合文件选择（file input 本来就很难受控）、简单无校验表单、和第三方非 React 控件集成。

大型表单用 Form 库（Formik、React Hook Form、async-validator）：RHF 默认非受控减少 rerender；需要逐字段联动时再局部受控。Vue 的 `v-model` 是受控的语法糖：`modelValue` + `update:modelValue`。

**追问标准答：**  
不要在同一个输入上既 `value` 又不管 `onChange`（只读却不像 disabled）。从非受控切受控（先无 value 后有 value）会警告，生命周期里要选一种。

---

**Q39. ★ 权限路由、动态菜单怎么做？**

**标准答案：**  
流程：登录成功拿到用户角色 / 权限码 → 用权限过滤「全量路由表」得到可访问路由 → 动态注册（Vue `router.addRoute`，React 用状态生成 `routes` 再交给 `useRoutes`）→ 侧边栏菜单用同一份过滤结果渲染 → 按钮级权限用指令或组件（无权限不渲染或禁用）。

刷新页面会丢内存里的动态路由，所以启动时：读本地 token → 拉用户信息（或读缓存）→ 再 addRoute → `next()` 放行。路由前置守卫里：无 token 去登录；有 token 无权限去 403；权限未加载完先等。

安全：前端隐藏不是安全，接口必须鉴权。前端只做体验和误入。

**追问标准答：**  
超管和普通用户不要发两套完全不同的前端包，用同一套代码 + 权限数据。菜单顺序、图标放配置，不要写死在组件里。

---

## 7. 工程化：Webpack、Vite、Babel

JD：熟悉构建工具，有项目构建与优化经验。

### 7.1 构建链路总览

```
源码（TS/JSX/Vue SFC/CSS）
    → 转译（Babel / SWC / esbuild：语法降级、JSX、TS 剥类型）
    → 模块图（import/export 依赖）
    → 转换（CSS Modules、资源 URL、env 替换）
    → 打包拆分（chunk）
    → 压缩、hash、source map
    → 产物（HTML 引用入口 JS/CSS）
```

**Babel 做什么 / 不做什么**

- 做：语法转换（箭头函数、可选链、TS 到 JS 的类型擦除若用 preset）、按配置的 polyfill 注入（`useBuiltIns`）。
- 不做：打包模块到一个文件（那是 bundler）；不自动让 IE 拥有 `fetch`（除非配了对应 polyfill）。

**Q40. ★★ Babel 和 Webpack / Vite 的关系？**

**标准答案：**  
Babel 是**转译器**：输入一段源代码字符串，输出另一段语法更旧或改写过的代码（箭头函数变 `function`、JSX 变 `createElement`、可选链变兼容写法）。它不负责把几十个文件拼成包，也不启动开发服务器。

Webpack / Vite 是**构建工具 / 开发服务器**：解析 `import` 画出模块图，调用 loader/plugin 处理文件。Webpack 里常用 `babel-loader` 调 Babel；Vite 开发阶段用 esbuild 转 TS/JSX，生产用 Rollup，需要时再接入 Babel（例如某些装饰器）。SWC 是 Babel 的 Rust 替代品，职责同类。

一句话：Babel 改「代码长什么样」；Webpack/Vite 管「文件怎么找、怎么拼、怎么 serve」。

**追问标准答：**  
`@babel/preset-env` 按 Browserslist 决定转哪些语法。`useBuiltIns: 'usage'` 才会按需注入 core-js。只配 preset 不配 polyfill，旧浏览器会 Syntax OK 但 `Promise` 仍不存在。

### 7.2 Webpack 核心概念

- Entry / Output / Loader（文件变模块） / Plugin（生命周期钩子）。
- 一切皆模块：JS、CSS、图片。
- `babel-loader`、`css-loader` + `style-loader`/`MiniCssExtractPlugin`。
- Tree Shaking：ESM 静态结构 + `usedExports` / `sideEffects` 标记。
- Code Splitting：`splitChunks`、动态 `import()`。
- HMR：开发时替换模块。
- `DefinePlugin` 注入 `process.env.NODE_ENV`，生产去掉开发分支。

**Q41. ★★ 如何减小包体积？（结合 Webpack）**

**标准答案：**  
先量后改。用 `webpack-bundle-analyzer`（Vite 用 `rollup-plugin-visualizer`）看哪一块最大，再动手。常见手段按收益排序：

1. **路由级懒加载**：`const Page = () => import('./Page')`，首屏不打后台页面。
2. **组件库按需引入**：不要 `import _ from 'lodash'`，用 `lodash-es` 单函数或 `import debounce from 'lodash/debounce'`。
3. **替换巨型库**：`moment` 换 `dayjs` 并去掉 locale 全量；图表按需注册。
4. **Tree Shaking**：必须 ESM；`package.json` 里正确的 `sideEffects`；生产 `mode: 'production'`。
5. **拆 chunk**：`splitChunks` 把 vue/react 打到 `vendor`，长期缓存；业务按路由拆。
6. **压缩**：Terser / esbuild minify；CSS 用 cssnano；CDN 开 gzip/brotli。
7. **图片不进包**：走 CDN，构建只留 hash 引用。
8. **externals + CDN**：体积下降但版本和缓存要自己管，不一定划算。

答面试要带一个数字：「moment 占 280KB，换成 dayjs gzip 后少 200KB」。

**追问标准答：**  
`sideEffects: false` 若乱标，会摇掉需要副作用的 CSS 导入。CSS 应标 `sideEffects: ['*.css']`。

---

**Q42. ★★ Loader 和 Plugin 区别？**

**标准答案：**  
Loader 是「文件转换器」：拿到一个模块的源码，返回转换后的源码。链路从右到左，例如 `use: ['style-loader', 'css-loader']` 先 css-loader 解析 `@import` 再 style-loader 注入。一个文件匹配一条或多条规则。

Plugin 是「编译生命周期钩子」：不针对单个文件，而是在整个 Compilation 过程中插队，例如 `HtmlWebpackPlugin` 生成 HTML 并注入 script、`DefinePlugin` 替换环境变量、`MiniCssExtractPlugin` 抽 CSS 文件、自定义在 `emit` 时写版本文件。

可以记：Loader 管「怎么把 x 变成 JS 模块」；Plugin 管「打包过程中额外干什么」。

**追问标准答：**  
Vite/Rollup 的 plugin 统一了转换和钩子，一个插件既可以 `transform` 某个文件，也可以 `generateBundle`。Webpack 5 的 Asset Modules 替代了 `file-loader`/`url-loader`。

### 7.3 Vite 为什么快

- 开发：原生 ESM，浏览器按需请求模块；依赖预构建用 esbuild（把 CJS 依赖打成 ESM）。
- 生产：Rollup 打包（或 Rolldown 等演进），仍要打包以优化请求数与 Tree Shaking。
- 冷启动快是因为不先打全量包。

**Q43. ★★ Vite 开发快，生产还要打包吗？为什么？**

**标准答案：**  
要打包。开发模式用原生 ESM：浏览器按需请求源文件，Vite 只转正在访问的模块，冷启动不必等全量打包，所以快。依赖（node_modules）用 esbuild 预构建成 ESM，避免上百个 CommonJS 小文件请求。

生产环境仍要打包，原因：

1. 即便 HTTP/2，成百上千个 ESM 请求仍有延迟和压缩效率问题。
2. 需要压缩、Tree Shaking、合理拆 chunk、CSS 提取与压缩。
3. 兼容性：生产目标浏览器可能不能直接跑全部新语法，需要统一转译。
4. 缓存策略依赖稳定的 contenthash 文件名。

所以「开发用 ESM 不打包」和「生产用 Rollup 打包」是两种产物形态，不是 Vite 生产不需要构建。

**追问标准答：**  
`vite preview` 预览的是打包结果，不是 dev server。Rolldown 等是生产打包器的演进，不改变「生产要打包」这个结论。

---

**Q44. ★★ 你如何选择 Webpack 还是 Vite？**

**标准答案：**  
新项目、Vue3 / React 标准栈，默认 Vite：开发体验和配置量都更优。保留 Webpack 的情况：已有大量自定义 loader/plugin 且没有 Rollup 等价物；强依赖 Module Federation 的旧方案（Vite 有 federation 插件但要评估成熟度）；构建流水线已深度绑定 Webpack 的公司基建。

迁移策略：先迁开发（收益最大），生产对比体积与运行时；准备一周双跑和回退。评估维度：冷启动时间、HMR、产物体积、插件缺口、团队熟悉度、CI 时长。

**标准答案示例：**  
后台从 Webpack 5 迁到 Vite，冷启动 45s → 3s。卡住的是一个读取 `process.cwd` 的旧 loader，改成 Vite plugin 的 `configResolved`。生产体积几乎没变，因为没先处理 moment，第二周才换成 dayjs。

**追问标准答：**  
微前端不是必须 Webpack。iframe、web component、Module Federation、qiankun 都能挂不同构建工具，成本在集成不是在选 bundler。

### 7.4 TypeScript（建议当作默认加分项）

岗位未写死 TS，但 3–5 年项目普遍使用。

- 结构类型；`interface` vs `type`。
- 泛型约束、`keyof`、条件类型入门。
- `any` 逃生舱 vs `unknown` + 收窄。
- 与 Vue `defineProps` / React `PropsWithChildren`。

**Q45. ★★ `any` 和 `unknown`？**

**标准答案：**  
`any` 关闭类型检查：赋给谁、调用什么方法都允许，等于把 TypeScript 局部关掉，错误会推迟到运行时。`unknown` 是「我不知道是什么」的安全类型：可以接任何值，但必须先收窄（`typeof`、`in`、自定义谓词、断言）才能当具体类型用。

```typescript
function parse(json: string): unknown {
  return JSON.parse(json);
}
const data = parse(text);
if (typeof data === "object" && data && "id" in data) {
  console.log((data as { id: string }).id);
}
```

团队 ESLint 禁 `any`。例外：第三方无类型、临时迁移。例外要写注释说明为什么，并尽快补 `.d.ts`。`as any` 链式调用是最差写法。

**追问标准答：**  
`never` 是不该发生的类型（穷尽检查）。`interface` 可声明合并，`type` 可以联合和映射，新代码两者都能用，对象形状用 interface 更常见。

### 7.5 模块化历史（简答即可）

IIFE → CJS（同步、运行时）→ AMD → ESM（静态、异步、树摇）。面试问「为什么 Tree Shaking 需要 ESM」。

---

## 8. 性能优化（岗位职责高频）

原则：**先测量，再优化，再验证。** 指标比「感觉快了」重要。

### 8.1 指标（Web Vitals）

| 指标 | 含义 | 粗优化方向 |
|------|------|------------|
| LCP | 最大内容绘制 | 英雄图、字体、服务端/SSR、CDN、不要用巨大轮播当 LCP |
| INP / FID | 交互到下次绘制 | 减主线程长任务、拆包、Web Worker |
| CLS | 布局偏移 | 图片宽高、字体 fallback、广告位占位 |
| TTFB | 首字节 | 网关、SSR 缓存、地域 |
| FCP | 首次内容绘制 | 关键 CSS、减阻塞 |

工具：Lighthouse、Performance 面板、WebPageTest、RUM（GA4 / 自建上报）。岗位若涉及独立站 / 投放，RUM 比实验室分数更有说服力。

### 8.2 常见优化清单（按链路）

**加载**

- 路由 / 组件懒加载；首屏与次屏分离。
- 图片：WebP/AVIF、`srcset`、懒加载、CDN、雪碧图仅小图标、SVG。
- 字体：`font-display: swap`、子集化、预加载少量关键字体。
- 预加载：`preload` LCP 图；`preconnect` 第三方源；不要全预加载。
- HTTP/2、压缩 gzip/brotli、缓存策略。
- SSR / SSG / ISR：首屏 HTML 直出；注意 hydration 成本。
- 第三方脚本：GTM 延迟、async、用 partytown 等（知道即可）。

**运行时**

- 长列表虚拟滚动。
- 防抖节流；被动监听 `{ passive: true }`。
- 避免巨大 JSON 阻塞；分页、增量。
- 动画走 transform；减少 layout thrashing。
- 内存：解绑事件、销毁编辑器 / 图表实例、SPA 泄漏（定时器、全局 store 订阅）。

**构建**

- 见 §7 包体积。
- CSS 关键路径：提取关键 CSS vs 全量 CSS；CSS 也会阻塞渲染。

### 8.3 面试题

**Q46. ★★ 从打开页面变慢开始，你的排查步骤？**

**标准答案：**  
按「复现 → 定性 → 定量 → 改一处验一处」来，不先堆优化项。

1. **复现环境**：用移动端 4G 节流、中端机或 CPU 6x slowdown。桌面宽带看不出 LCP 问题。
2. **Network**：看瀑布。首字节 TTFB 是否超过 600ms（后端/CDN）；是否有巨大未压缩资源；是否串行阻塞（CSS/JS 没并行）；是否重复请求；缓存是否全是 `from disk` 还是每次 200。
3. **Performance 面板**：有没有超过 50ms 的长任务；layout 次数；JS 编译执行占比；GC。
4. **Lighthouse / Web Vitals**：先看是 LCP、INP 还是 CLS 差，对症下药。LCP 看是图、字还是 HTML 晚到；INP 看主线程；CLS 看无尺寸图片和插入广告。
5. **定一个指标**：例如「移动端 LCP 从 4.0s 到 2.5s 以下」。每次只改一类问题（先图，再 JS 体积），避免无法归因。

**追问标准答：**  
实验室分数和 RUM 会不一致。要以真实用户 P75 为准。投放页还要看 WebView 和微信。

---

**Q47. ★★ 首屏优化做过哪些？请用数据说话。**

**标准答案示例（可替换成自己的项目）：**  
现象：运营活动 H5 在 iPhone 12 + 4G 下 Lighthouse 移动端 LCP 4.2s，跳失高。  
基线：LCP 元素是首屏全宽 PNG 轮播（2000px，800KB），主包 1.8MB，字体阻塞渲染。  
手段：轮播改一张 WebP 英雄图并 `<link rel="preload">`；其余图 `loading="lazy"`；路由懒加载把首屏 JS 降到 gzip 420KB；中文字体子集化 + `font-display: swap`；图片写死宽高。  
结果：LCP 2.1s，CLS 0.18 → 0.04。  
副作用：运营不能把五张图都放首屏，产品接受。  
若没有自己的数字，至少按这个结构讲：现象、指标、手段、结果、副作用。罗列 20 条技术名词但没有数字，视为未答完。

**追问标准答：**  
SSR 能降 FCP/LCP，但 hydration 会增加 INP。投放页若无 SEO 需求，预渲染静态 HTML 往往比全量 SSR 更简单。

---

**Q48. ★★ 十万行列表怎么渲染？**

**标准答案：**  
浏览器和框架都不能真的挂十万个 DOM 节点。方案按产品选：

1. **分页**：后台表格首选，实现简单，和服务端一致。
2. **虚拟列表**：只渲染可视区 + 上下各几屏缓冲。滚动时计算 `startIndex`，用一个占位总高度撑开滚动条，绝对定位可见行。固定高度最简单；动态高度要测量缓存（react-window、vue-virtual-scroller）。
3. **时间分片**：数据要一次性在内存，但渲染分批 `requestAnimationFrame`，体验仍差，只作过渡。

注意：虚拟列表的白屏（滚动太快缓冲不够）、滚动位置跳动（动态高度估算不准）、无障碍（读屏只能看到可见行）、搜索/跳转到第 N 行要自己算 offset。

**参考实现（固定高度口述级）：**

```javascript
function visibleRange(scrollTop, viewportH, itemH, total) {
  const start = Math.max(0, Math.floor(scrollTop / itemH) - 5);
  const count = Math.ceil(viewportH / itemH) + 10;
  const end = Math.min(total, start + count);
  return { start, end, offsetY: start * itemH, totalH: total * itemH };
}
```

**追问标准答：**  
「用 Canvas 画表格」适合金融行情，交互和 a11y 成本高。不要一上来就 Canvas。

---

**Q49. ★★ 图片优化怎么做？**

**标准答案：**  
按「格式、尺寸、加载时机、缓存」四步。

1. **格式**：照片用 WebP/AVIF，不行回退 JPEG；图标用 SVG；不要用 PNG 存照片。
2. **尺寸**：显示 375px 宽不要下 4000px 原图。用 `srcset` + `sizes` 让浏览器选档。CDN 开宽高参数（如 `?w=750`）。
3. **时机**：首屏 LCP 图 `preload` 且不要 lazy；屏外 `loading="lazy"` 或 IntersectionObserver。占位用固有宽高或 `aspect-ratio` 防 CLS，可用模糊缩略图。
4. **缓存与压缩**：CDN、合理 Cache-Control、构建时 imagemin。雪碧图只适合超小图标且不改色；Icon Font 对多色和无障碍不友好。

移动端还要算流量：活动页 10 张图就能吃掉数 MB。

**追问标准答：**  
`background-image` 不能 `srcset`，LCP 图尽量用 `<img>`。CSS 大图无法被浏览器按 LCP 优化得那么好。

---

**Q50. ★★★ hydration 很慢怎么办？（若项目有 SSR）**

**标准答案：**  
Hydration 是客户端 JS 接管服务端 HTML：要下载、解析、执行整棵组件树并绑事件。慢的表现是 FCP 很快但几秒内无法点击（INP 差），或 hydration mismatch 整页重渲染。

手段：

1. **少 hydrate**：岛屿架构 / 选择性 hydration，页头页脚静态，只有轮播、表单 hydrate。
2. **减客户端 JS**：首屏不要挂编辑器、图表、全量 UI 库。
3. **避免 mismatch**：服务端与客户端初次 render 必须同一份数据（时间、随机数、`window` 判断会导致不一致）。
4. **别重复请求**：SSR 已取的数据注入到页面，客户端不要立刻再打一遍同样的接口。
5. **流式 SSR / 逐步 hydrate**：React 18 streaming、Vue streaming，先出 HTML 再分块 hydrate。

**追问标准答：**  
纯营销落地页用 SSG/预渲染往往比 SSR+hydration 更合适。有 SEO + 强交互才上 SSR。

---

## 9. Git 与团队协作

JD：熟练 Git，能协作。不要只问 `git clone`。

### 9.1 必须熟练的命令与场景

- `add` / `commit` / `push` / `pull` / `fetch`
- 分支：`checkout` / `switch` / `merge` / `rebase`
- `stash`、`cherry-pick`、`reset`（软硬混合）、`revert`（公开历史用 revert）
- `log`、`blame`、`bisect`（回归定位加分）
- 冲突：理解双方改同一 hunk；二进制冲突找设计 / 产物是否误入仓库

**rebase vs merge**

- merge：保留分叉，安全，适合主干。
- rebase：线性，改写历史，**不要 rebase 已推送的共享分支**。
- 面试要说清风险，而不是「rebase 更高级」。

**Git Flow 类流程（按团队实际）**

- `main` 保护、MR/PR、CI 必须过、feature 短分支。
- commit message：约定式（feat/fix/chore）便于 changelog。
- `.gitignore`：不要提交 `node_modules`、`.env`、编辑器目录。

**Q51. ★ 已经 commit 尚未 push，改最后一次说明？**

**标准答案：**  
`git commit --amend -m "新说明"` 或 `git commit --amend --no-edit` 把漏掉的文件加进最后一次提交。仅当该 commit 还没 push，或确定是你自己的个人分支且没有别人基于它工作时才能 amend。已经 push 到共享分支再 amend 需要 `git push --force`，会改写历史，协作分支禁止。改公开历史用 `git revert` 出一次新提交。

**追问标准答：**  
`git reset --soft HEAD~1` 撤销 commit 但保留暂存；`--mixed`（默认）保留工作区改动；`--hard` 丢掉改动，危险。未 push 才能 reset「最后一次」的公共语义。

---

**Q52. ★★ 误提交密钥怎么办？**

**标准答案：**  
第一优先：**立刻轮换密钥**（云厂商控制台作废旧 key、发新 key）。历史里删文件不能当作密钥没泄漏，因为 fork、CI 日志、别人已经 pull 都可能拿到。

然后清历史：用 `git filter-repo` 或 BFG 从全部 commit 里去掉该文件，force push 所有分支和 tag，通知同事重新 clone。GitHub 还要作废 Actions 缓存。`.gitignore` 补上 `.env`、密钥文件，并加 pre-commit 扫描（gitleaks）。

只 `git rm` 再 commit 一次：**密钥仍在旧 commit 里**，等于没处理。

**追问标准答：**  
哪怕只在公司内网 repo，也要轮换。扫描工具误报可以忽略，真密钥不行。

---

**Q53. ★★ 生产 bug 要热修，功能分支还在开发？**

**标准答案：**  
从当前生产对应的主干（`main` / `release`）拉 `hotfix/xxx`，只含修复，走加急 CR 和 CI，发版。然后把 hotfix **merge 或 cherry-pick 回正在开发的分支**，否则下次发版会把生产修复冲掉。不要在未完成的大功能分支上直接发生产。不要在 hotfix 里夹带需求。

若用 Git Flow：`hotfix` 从 `main` 拉，结束时同时合入 `main` 和 `develop`。

**追问标准答：**  
线上回滚优于在热修里赌大改。功能开关可以关有问题的模块而不回滚整个包。

### 9.2 Code Review 与规范（对应职责）

Review 看什么：正确性、可读性、边界、性能、安全、测试、是否符合目录与命名约定。

**Q54. ★★ 你在 Review 里坚持过哪些必须改的点？**

**标准答案示例：**  
格式不做人肉 Review，ESLint / Prettier 进 CI。我卡的是正确性和风险：

1. 密钥、`.env`、内网地址进仓库，必须改。
2. 列表无稳定 key、表单输入会错位，必须改。
3. 未转义的 `innerHTML` / `v-html` / `dangerouslySetInnerHTML` 喂用户内容，必须改或消毒。
4. 异步失败被 `catch` 后只 `console.log`，用户无反馈，必须改。
5. 组件 500+ 行且请求、UI、校验搅在一起，要求拆纯函数便于测。
6. 任意 `any`、把 `as any` 当通行证，必须补类型。
7. 无障碍：可点击的 `div` 没有键盘支持，公共组件必须改 `button`。

差答案是「看看命名和缩进」。好答案要具体到「哪一类问题会进生产事故」。

**追问标准答：**  
Review 要限时，一次 PR 不超过 400 行有效代码，否则拆。只批不教会让团队重复踩坑，卡点应写进规范文档。

---

## 10. 架构、选型、联调与质量

对应职责：技术选型、架构、高可用可扩展、接口约定、可测试代码、推动规范。

### 10.1 前端架构怎么谈

3–5 年不要求从零造框架，要求**能画一张图并解释取舍**：

```
页面 / 路由
  → 状态（服务端状态 vs 客户端状态）
  → API 层（类型、错误、鉴权、重试）
  → 领域模块（用户、订单、内容）
  → 基础设施（埋点、日志、权限、i18n、主题）
```

- 目录按领域还是按技术分层：中大型建议领域优先。
- 环境：dev / staging / prod；配置与密钥。
- 错误边界：React Error Boundary / Vue `errorHandler`；监控（Sentry）。
- 可扩展：多品牌、多语言、多站点时配置驱动，而不是复制仓库。

**Q55. ★★ 你做过的技术选型：候选、比较维度、结论、后悔点。**

**标准答案示例：**  
候选：后台状态管理在 Vuex 4 和 Pinia 之间。维度：TS 推断、模块嵌套心智、文档与招聘、迁移成本、DevTools。结论：新模块用 Pinia，Vuex 模块不强制重写，双栈共存一季度。后悔点：没先定「服务端状态不进 Pinia」，后来列表缓存和 Pinia 重复，又引入一层请求库。选型不是选最流行，是选约束下成本最低，并且预留后悔的退路。

比较维度清单：团队熟悉度、生态与招聘、TypeScript、性能、构建速度、迁移成本、长期维护人数。

**追问标准答：**  
「我们用了 React 所以高可用」不是选型。要能说出不选某方案的原因。

---

**Q56. ★★ 前端如何配合「高可用」？**

**标准答案：**  
前端不负责机房，但能决定「挂了用户看到什么、能不能回滚、会不会雪崩」。

1. **静态资源**：CDN 多源、文件名哈希不可变、HTML 可回源；CDN 挂了有备用域名。
2. **发布**：可在 5 分钟内回滚上一版本；功能开关关掉有问题模块。
3. **接口**：超时、重试有上限、幂等；网关 5xx 展示降级页而不是白屏；核心链路（登录、下单）单独监控成功率。
4. **第三方脚本**：GTM、统计、客服挂件用异步且失败隔离，不要让第三方打断主流程。
5. **监控告警**：JS 错误、接口错误率、Web Vitals 跌破阈值叫醒人。
6. **容量**：活动页防缓存击穿、图片走 CDN，避免把流量打到源站 Node。

不要答「我们用了 React / 微服务」。

**追问标准答：**  
灰度：按百分比或白名单放量。SSR 的高可用还包括 Node 进程管理和 SSR 降级到 CSR。回滚演练比文档重要。

### 10.2 与设计师协作（还原度）

- 设计规范：间距 8 倍数、色板、字体层级；用 Design Token。
- 还原：标注工具、1px 对齐、不同 dpr、暗色模式（若有）。
- 独立 Desktop / Mobile 稿：两套布局组件，而不是只改 `font-size`。
- 分歧：实现成本（动画、模糊、滚动吸附）摆数据与排期，给替代方案。

**Q57. ★★ 设计稿和实现差 2px 你怎么处理？**

**标准答案：**  
先定性：用户可感知吗？2px 在 1x 屏可能对齐问题，在 3x 屏可能是亚像素取整。步骤：

1. 同一浏览器、同一缩放 100%、关浏览器默认最小字号，对比设计稿。
2. 查全局样式：`box-sizing`、`line-height`、user agent 的 `button` 边框、flex 的 gap 与设计 8 点网格是否一致。
3. 字体：设计用了未授权字体或字重 500 被回退成 400，基线会偏。
4. 若全站系统性偏差（所有卡片都多 2px padding），改 token，不要每个页面补。
5. 若是设计稿自己没对齐网格，提给设计改稿，而不是前端用负 margin 硬修。

态度：对可感知差异负责；对视网膜下 1 物理像素争论记录规范，避免无限返工。给替代方案而不是直接说「做不到」。

**追问标准答：**  
走查清单：间距、对齐、色值、圆角、阴影、空态、超长文本、hover、禁用、错误态、iPhone 安全区。PC / Mobile 两套稿就两套走查。

### 10.3 与后端协作

- 先定契约：OpenAPI / 类型生成（openapi-typescript）、字段命名、分页、错误码、鉴权头、时间与时区、金额用整数分。
- Mock：MSW、apifox、契约测试；前端不阻塞。
- 变更：版本号或兼容字段；禁止静默改语义。
- 联调：跨域、Cookie、网关前缀、环境 hosts。

**Q58. ★★ 接口还没好，你如何不耽误进度？**

**标准答案：**  
契约先行。和后端开 30 分钟定：路径、方法、鉴权、分页结构（`list+total` 还是 `cursor`）、错误码枚举、时间 ISO8601、金额用分（整数）。把契约写进 OpenAPI / Apifox，前端用 MSW 或 mock 服务按契约开发页面和交互。用 openapi-typescript 生成类型，页面不手写 `any`。

后端延期时前端仍可完成：加载/空/错/满四种 UI、权限分支、埋点。联调只剩对数据。字段变更：新增兼容；破坏性变更走版本或双写字段并给下线日期。发现不一致：类型 CI 或运行时 zod 校验至少一种，避免「联调当天才发现少字段」。

**追问标准答：**  
Mock 要能模拟 401/500/空列表，不能只有 happy path。网关前缀、Cookie 跨域放到 mock 之外的环境问题清单里。

### 10.4 可测试性

- 单测：纯函数、工具、store reducer。
- 组件测：用户行为（Testing Library），少测实现细节。
- E2E：关键路径（登录、下单）少量稳定用例。
- 3–5 年不要求 90% 覆盖率，要求**知道测什么、怎么把逻辑从 UI 剥离**。

**Q59. ★ 一段难测的代码怎么改？**

**标准答案：**  
难测通常是因为副作用和 UI 缠在一起：函数里直接 `fetch`、`Date.now()`、`Math.random()`、读 `window`、操作 DOM。改法是依赖注入并把规则变成纯函数。

```javascript
// 难测
async function submit() {
  const price = Math.round(input.value * 1.13);
  await fetch("/api/order", { method: "POST", body: JSON.stringify({ price }) });
}

// 可测：规则纯函数 + 请求可替换
export function calcPrice(cents, taxRate, round = Math.round) {
  return round(cents * taxRate);
}
export async function submitOrder(cents, post = fetch) {
  const price = calcPrice(cents, 1.13);
  return post("/api/order", { method: "POST", body: JSON.stringify({ price }) });
}
```

单测覆盖 `calcPrice` 的进位和边界；组件测模拟用户点击；E2E 只覆盖下单黄金路径。3–5 年不追求 90% 覆盖率，但要能把逻辑从 Vue/React 组件里抽出来。

**追问标准答：**  
时间用注入 `now` 或假时钟，不要在测试里 `sleep(1000)`。随机数注入 seed。

---

## 11. 场景题与项目深挖（强烈建议用真项目）

让候选人打开自己的项目讲。面试官准备下面清单，逐项追问。

### 11.1 项目介绍结构（STAR + 技术）

1. 背景与用户
2. 你的职责边界（别把团队成果全揽）
3. 架构图与技术栈
4. 最难的 1–2 个问题
5. 指标（性能、稳定性、交付周期）
6. 如果重做会怎么改

### 11.2 高频场景

**场景 A：活动 H5 / 投放落地页**

加载、微信分享鉴权、适配、统计、回退刷新丢参数。

**场景 B：中后台表格**

列配置、大数据量、筛选联动、权限按钮、导出、与后端分页一致性。

**场景 C：电商 / 交易（若业务相关）**

SKU、库存竞态、价格精度、支付回跳、订单状态机、幂等。

**场景 D：建站 / CMS / 多品牌**

页面 schema、预览与发布、多语言、缓存与回滚、设计稿还原。与仓库中独立站平台能力同构，可作案例。

**Q60. ★★★ 设计一个前端监控方案。**

**标准答案：**  
监控四类数据，一条上报链路。

1. **稳定性**：`window.onerror`、`unhandledrejection`、资源 `error`（script/img）、框架 `errorHandler` / Error Boundary。带上 `release` 版本和 sourcemap 才能还原堆栈。
2. **性能**：Web Vitals（LCP、INP、CLS）用 `web-vitals` 库；接口耗时在请求封装里打点。
3. **接口**：状态码、业务 code、耗时、失败率。核心接口单独看板。
4. **业务漏斗**：曝光、点击、下单成功。要和产品漏斗同一套事件名。

链路：SDK 采集 → 本地队列 + 抽样（例如错误 100%、性能 10%）→ `sendBeacon` / `fetch keepalive` 发到收集服务 → 脱敏（去掉 token、身份证）→ 存储与告警（错误突增、LCP P75 超 2.5s）。开发环境不上报或单独项目。

**追问标准答：**  
sourcemap 不要公网可下载，只给监控服务。PII 脱敏在上报前做。采样过低会看不见长尾机型。

---

**Q61. ★★ 登录态过期、多页签互踢，怎么做？**

**标准答案：**  
Access Token 短效（例如 15 分钟）放内存或非 HttpOnly 方案按安全策略；Refresh Token 放 HttpOnly Cookie。请求拦截器遇到 401：把后续请求推进队列，只发一次 refresh，成功后重放队列；refresh 也 401 则清登录态跳登录页。

多页签：`BroadcastChannel` 或 `localStorage` 的 `storage` 事件广播 `logout` / `login`，一个页签退出则全部退出。不要每个页签各自刷 refresh 把 refresh token 打成一次性时互相踢——用 Web Lock 或「主页签刷新」。产品要决定：多端互踢（新登录作废旧会话）还是允许多端。无感刷新适合后台；C 端支付页更适合过期弹登录，避免静默失败。

**追问标准答：**  
并发 401 若每人 refresh 一次会打爆认证服务。必须单飞（in-flight promise 复用）。

---

**Q62. ★★ 微前端了解多少？**

**标准答案：**  
微前端把多个可独立发布的前端应用拼到一个页面。常见实现：

| 方案 | 隔离 | 代价 |
|------|------|------|
| iframe | 最强 | 路由、高度、通信、SEO 差 |
| qiankun / single-spa | JS 沙箱 + 样式 scoped | webpack 运行时、版本依赖 |
| Module Federation | 运行时共享依赖 | 构建耦合、版本对齐 |
| Web Components | 样式封装 | 生态和框架集成要包一层 |

**什么时候不要用：** 团队小于 10 人、只是目录拆分就能独立开发、构建和联调成本已经高于发布冲突。微前端解决的是组织问题（多团队独立发版），不是性能问题（通常更慢、重复依赖）。

痛点：样式泄漏、公共依赖多份 React、路由抢 history、联调要起多个服务、线上版本组合爆炸。

**追问标准答：**  
独立站多品牌更适合「配置驱动的一套代码」而不是微前端。真要拆，先拆独立域名子应用，成本最低。

---

## 12. 面试流程、题单与评分

### 12.1 建议时长分配（60 分钟）

| 时段 | 内容 | 题号池 |
|------|------|--------|
| 0–5 min | 自我介绍、项目索引 | §11 |
| 5–20 min | JS + 浏览器 + CSS（按履历偏科） | Q1–Q26 抽 4–5 题 |
| 20–35 min | 主框架原理 + 生态 | Q27–Q39 抽 4 题 |
| 35–48 min | 工程化 + 性能（要项目证据） | Q40–Q50、Q55 |
| 48–58 min | 协作 / Git / 设计还原 / 联调 | Q51–Q59 |
| 最后 | 反问 | 候选人应问业务、技术债、指标、团队规模 |

电话面可只做 Q13、Q18、Q28 或 Q32、Q41、Q46。

### 12.2 评分参考（每项 1–5）

| 维度 | 3 分（达标） | 5 分（强） |
|------|--------------|------------|
| 语言基础 | ES6、异步、闭包正确 | 能关联引擎/框架实现 |
| 框架 | 生态熟练，原理能讲大意 | 能对比、能讲优化与取舍 |
| 工程 | 能讲构建与拆包 | 有构建耗时/体积治理案例 |
| 性能与兼容 | 知道 Vitals 和常见手段 | 有指标闭环和兼容策略 |
| 协作 | Git 常用、能 Review | 推动规范、契约、还原流程 |
| 沟通 | 结构清楚 | 主动画图、承认未知、讨论权衡 |

**风险信号**

- 只会背「虚拟 DOM 更快」。
- 性能优化罗列 20 条但没有任何数字。
- 三年经验说不清一次线上事故或一次回滚。
- 贬低设计师 / 后端，无协作方法论。
- 主框架原理完全空白（与 JD「理解核心原理」直接冲突）。

**通过线（3–5 年）**

- 主框架原理达到 ★★。
- 至少 1 个性能或构建优化的闭环案例。
- CSS 布局 + 移动端 1px/适配能讲通。
- Git 协作无危险操作（乱 force push 共享分支）。

### 12.3 面试官速查：按 JD 一条对一题

| JD 条款 | 最少要问 |
|---------|----------|
| 计算机基础 + HTML/CSS/JS | Q2、Q7、Q18 |
| 至少一门框架 + 原理 | Vue: Q28 Q30；React: Q32 Q33 |
| 生态 Vuex/Pinia/Redux/Router | Q39 或「全局状态何时不该用」 |
| Webpack/Vite/Babel | Q40、Q43 |
| 项目构建与性能 | Q41、Q46、Q47 |
| Git 协作 | Q52 或 Q53 |
| 跨浏览器跨端 | Q10、Q25、Q9 |
| 沟通责任心 | Q57 + 项目冲突故事 |
| 分析与调研 | Q55、最近一次官方文档/源码阅读 |

### 12.4 候选人反问题（优质信号）

- 前端团队规模、主技术栈版本、有无 Design System。
- 发布频率、是否有预发、监控与 oncall。
- 这次招聘是补位还是新业务（独立站 / 后台 / 活动）。
- 成功标准：入职 90 天交付什么。
- 性能指标是否纳入考核（LCP 等）。

---

## 13. 四周自学路径

按每天 1.5–2 小时估计。已有项目经验者把「写 demo」改成「给现有项目补一次优化并记录指标」。

### 第 1 周：语言与浏览器

- 重读：闭包、原型、this、事件循环；自己写 debounce、Promise.all、并发池。
- 跟一遍「URL 到渲染」；Performance 面板录一次自己站点。
- CSS：Flex + Grid 各做一个后台布局；移动端 1px 与 `min-width: 0` 实验。
- 输出：用自己的话写一页「事件循环 + 重排重绘」。

### 第 2 周：主框架原理

- Vue 主：读响应式 effect 依赖收集；对比 Vue2 defineProperty。
- React 主：Hooks 链表直觉；`memo` 实验；解释一次闭包陷阱。
- 实现：迷你响应式 **或** 迷你 `useState`（二选一做深）。
- 路由懒加载接到真实项目。

### 第 3 周：工程与性能

- 用 analyzer 分析自己的构建产物，砍掉一个最大依赖或改懒加载。
- 读懂 Vite 开发 vs 生产差异；配一次 Browserslist。
- Lighthouse 跑移动端，改 LCP / CLS 中的一项并记录分数。
- 图片与缓存策略过一遍。

### 第 4 周：质量、协作、模拟面

- 给自己项目补 ESLint + 一次有质量的 PR 描述。
- 画架构图；准备 2 个 STAR 故事（事故、还原、联调、性能各尽量覆盖）。
- 按 §12.1 计时模拟；录音检查是否空洞。
- 弱项回炉：哪门 ★★ 题讲不利索就只补那一块。

### 推荐材料（少而精）

- MDN：HTTP 缓存、CORS、CSS 层叠。
- 你所用框架的**官方文档「深入」章节**（Vue Reactivity、React Rendering）。
- Web.dev：Web Vitals。
- 构建：Vite 官方 Why Vite；Webpack 概念页（Entry/Output/Loader/Plugin）。
- 源码：不要一上来读全仓库。带着一个问题读（`key` 怎么用、`effect` 怎么收集）。

---

## 附录 A. 白板手写清单与标准实现

现场可能出的题。debounce / 深拷贝 / 并发池 / 迷你响应式 / instanceof 的完整实现见 Q21、Q12、Q19、Q31、Q15。下面补其余标准答案。

### A1. Promise.all

```javascript
function promiseAll(promises) {
  return new Promise((resolve, reject) => {
    const list = Array.from(promises);
    if (list.length === 0) return resolve([]);
    const result = new Array(list.length);
    let left = list.length;
    list.forEach((p, i) => {
      Promise.resolve(p).then(
        (value) => {
          result[i] = value;
          if (--left === 0) resolve(result);
        },
        reject
      );
    });
  });
}
```

要点：空数组立刻 resolve；按下标写回保证顺序；任一失败立刻 reject。

### A2. 数组去重 / flatten

```javascript
// 去重。原始值 O(n)；对象去重要按引用或指定 key
function unique(arr) {
  return [...new Set(arr)];
}

// flatten。深度默认 Infinity
function flatten(arr, depth = Infinity) {
  return depth > 0
    ? arr.reduce(
        (acc, cur) =>
          acc.concat(Array.isArray(cur) ? flatten(cur, depth - 1) : cur),
        []
      )
    : arr.slice();
}

// 或：arr.flat(depth)
```

复杂度：unique 平均 O(n)；flatten 与元素总数成线性。循环引用数组要 WeakSet 防死循环。

### A3. 发布订阅 EventEmitter

```javascript
class EventEmitter {
  constructor() {
    this.events = Object.create(null);
  }
  on(type, fn) {
    (this.events[type] || (this.events[type] = [])).push(fn);
    return this;
  }
  once(type, fn) {
    const wrap = (...args) => {
      this.off(type, wrap);
      fn.apply(this, args);
    };
    wrap.fn = fn;
    return this.on(type, wrap);
  }
  off(type, fn) {
    if (!fn) {
      delete this.events[type];
      return this;
    }
    this.events[type] = (this.events[type] || []).filter(
      (cb) => cb !== fn && cb.fn !== fn
    );
    return this;
  }
  emit(type, ...args) {
    [...(this.events[type] || [])].forEach((fn) => fn.apply(this, args));
    return this;
  }
}
```

要点：`emit` 时拷贝数组，避免监听器里 `off` 影响遍历；`once` 要能用原函数 `off`。

### A4. 简易 `new`

```javascript
function myNew(Ctor, ...args) {
  const obj = Object.create(Ctor.prototype);
  const result = Ctor.apply(obj, args);
  return result !== null && (typeof result === "object" || typeof result === "function")
    ? result
    : obj;
}
```

步骤：创建对象并挂原型 → 用对象当 this 调构造函数 → 若构造函数返回对象则用该返回值，否则用新对象。

### A5. CSS 三栏布局（圣杯 / Flex / Grid）

```css
/* Flex，最常用 */
.row { display: flex; }
.side { width: 200px; flex-shrink: 0; }
.main { flex: 1; min-width: 0; }

/* Grid */
.row {
  display: grid;
  grid-template-columns: 200px 1fr 200px;
}
```

垂直居中见 Q8，1px 见 Q9。

### 手写时的加分习惯

- 先问输入边界（null、非数组）。
- 写出时间复杂度。
- 说明浏览器环境假设。
- 写完自己举一个失败用例。

---

## 附录 B. 学习自测（闭卷）

能在不看答案的情况下讲 3 分钟即视为过关。对照题号：1→Q18，2→Q28 或 Q32，3→Q43，4→Q46/Q47，5→Q7/Q9，6→B4，7→Q51/Q53，8→Q58。

1. 事件循环 + 一段含 `async` 的打印顺序。
2. Vue3 Proxy 依赖收集 **或** React Hooks 为什么不能写在 if 里。
3. Vite 开发为什么快、生产为什么仍打包。
4. LCP 是什么、你如何提高它。
5. 移动端 1px 与 flex 子项溢出。
6. 一次真实的性能或兼容问题闭环。
7. Git：revert 与 reset 何时用。
8. 接口契约如何让前后端并行。

---

## 附录 C. 岗位职责 → 学习产出对照

把「能说」变成「有物证」，面试更稳。

| 职责 | 建议准备的物证 |
|------|----------------|
| 开发维护 Web 产品 | 1 个模块的设计：路由、状态、错误处理 |
| 技术选型与架构 | 一页对比表 + 架构草图 |
| 性能优化与高可用 | Lighthouse 截图或 RUM 前后数据 |
| 设计稿还原 | 走查清单、token、PC/Mobile 差异说明 |
| 前后端联调 | OpenAPI / Mock 流程、错误码表 |
| 高质量可测试代码 | 一段抽离的纯函数测试、CR 评论示例 |
| 兼容 PC/移动 | 浏览器矩阵、1px/vh/微信坑位笔记 |
| 跟踪新技术 | 最近一次落地（如 Vite、Pinia、React 18）与回退计划 |

---

## 附录 D. 术语速查

| 术语 | 一句话 |
|------|--------|
| 关键渲染路径 | 从收到字节到首次绘制必须完成的工作 |
| 强缓存 / 协商缓存 | 直接用本地 vs 问服务器 304 |
| 合成层 | GPU 单独合成的层，动画更平滑，内存更贵 |
| 树摇 | 删除未使用的 ESM 导出 |
| Hydration | SSR HTML 与客户端框架接管绑定 |
| BFC | 独立块级格式化上下文，影响浮动与 margin |
| Fiber | React 可中断的更新链表单元 |
| Proxy | 对象级拦截，Vue3 响应式基础 |
| Web Vitals | Google 推荐的用户体验核心指标 |
| SameSite | 限制跨站带 Cookie，缓解 CSRF |

---

*本文档按「必备条件 + 主要职责」编写。v1.1 起每道题给出完整标准答案与必要参考实现，面试官可对照评分，学习者可复述后用自己的项目替换案例。*
